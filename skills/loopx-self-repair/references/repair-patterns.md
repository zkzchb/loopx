# LoopX Self-Repair Patterns

Use this file as a compact diagnosis table. Add a row whenever a real incident
teaches a reusable control-plane lesson.

| Pattern | Symptoms | Evidence To Read | Likely Root | Durable Repair |
| --- | --- | --- | --- | --- |
| `skill_discovery_split_ownership` | Duplicate skill names, conflicting PR-review routes, or canonical and legacy aliases appear together. | Enumerate discovered roots, resolve directory symlinks, compare skill hashes, managed markers, install receipts, and generated metadata. | Workflow and command installers wrote independently to overlapping host roots; dedupe was optional, omitted the bare entry name, or retired copies without proving a replacement. | Repair the shared installer reconciliation and every active installation path; preserve user changes and rich workflows, retire managed aliases from the Codex picker, test repeated installs and custom profiles, then verify a fresh host catalog. Do not treat deleting one visible duplicate or changing invocation policy as a durable fix. |
| `skill_discovery_scope_eligibility_conflation` | Project delivery rejects a reusable workflow, and adding a project marker unexpectedly removes connection, repair, or review instructions from global installation. | Canonical scope markers, default shell/CLI and packaged install output, project-copy readback, and doctor required workflows. | One marker was treated as both exclusive project eligibility and default discovery; content richness was mistaken for project authority. | Declare reusable workflows global and capability-local workflows project; accept both explicit declarations for project copies while rejecting missing/unknown markers. Preserve global command routes, existing activation gates, and packaged resource parity. Never repair project delivery by hiding bootstrap or repair instructions from unconnected projects. |
| `replan_successor_receipt_lost_on_refresh` | A validated successor closes replan briefly, but the next refresh rotates the periodic obligation over the same old runs. | Exact successor obligation id, shared frontier transition ACK, refresh run/index ACK, and next periodic window. | Writeback discarded the accepted transition once the read model no longer had an open obligation, so no durable checkpoint reset the review window. | Preserve the shared reducer’s accepted successor delta in the normal refresh and retain exact Turn-generation filtering. Cover CLI successor creation, refresh persistence, next-window suppression and full new-window rearming; do not add another ACK ritual or duplicate successor. |
| `invalid_identity_normalization_alias` | Two malformed binding ids appear to match after normalization, allowing unbound blocker evidence to count. | Raw synthetic ids, normalized results, and the same record through writer validation and the typed history projection. | Equality of two failed normalizations was mistaken for a valid identity match. | Require successful expected-id normalization before comparing identities; preserve oversized-id invalidity during compact transport and test malformed pairs independently from valid Todo/replan bindings. |
| `causal_wait_coverage_gap` | One waiting route hides another open acceptance binding, or adding unrelated waiting Todos changes defer into replan. | Replay the same canonical Todo/vision facts through the real CLI with absent/runnable/waiting successors, varied display sizes, and reordered unrelated rows. | A union of related waits was treated as coverage of every causal binding, or compact display absence was treated as canonical absence. | Derive per-binding wait coverage from canonical Todo rows before display slicing; reuse resume/ownership evaluators and directed successor lineage. Keep incomplete evidence distinct from proven absence, and prove ordinary authoring and lifecycle paths before adding separately maintained declarations. |
| `narrative_delivery_authority_leak` | Rewording a run label turns resolved work into a blocker or preparation into a required follow-through; untyped history accumulates delivery debt. | Explicit outcome/kind/scale, scoped progress observation, compact history, and status/quota decisions before and after narrative-only mutations. | Keyword inference and unknown-as-failure streaks promoted narrative into machine authority. | Use explicit typed semantics and the existing scoped blocker predicate; unknown breaks evidence streaks. Delete keyword fallbacks, preserve labels for display, and test both negative narrative mutations and positive typed obligations through the CLI. |
| `periodic_report_todo_log_editorial_gap` | A stage report is generated and frozen, but the artifact is an English Todo chronology, report-building work displaces business findings, or the page lacks a clear overview-to-depth analysis mainline. | Frozen artifact and language, projected completed/open Todo facts, report-meta action kinds, editorial source and section order, cause/boundary details, fact references, and approval-gate creation time. | The governed consumer treated durable Todo prose as audience-ready copy and let a renderer infer narrative structure from timestamps or generic content kinds. Generation correctness therefore proved persistence and authority, but not editorial quality. | Freeze a compact public-safe fact request for the exact intent, require Agent-authored Chinese editorial with a typed `overview -> problem map -> causal analysis -> coverage/actions -> next actions` contract, validate language density and fact lineage in the consumer, and create no frozen artifact or approval gate until the editorial response passes. Keep report-meta work outside the business narrative and retain publication as a later exact-payload authority gate. |
| `periodic_report_stage_boundary_premature_promotion` | A periodic report is generated after setup work, several ordinary Todo completions, or a replan even though the intended analysis or delivery vision remains open. | Goal-vision revision, material outcome checkpoint and evidence refs, replan trigger kind, accepted semantic delta, successor frontier ownership or terminal Goal, report trigger receipt, and rendered-artifact count. | Runtime aggregation treated Todo volume or any replan as proof of a meaningful stage boundary instead of distinguishing success-path vision closure from recovery and continuation planning. | Derive stage completion as the strict successful sibling of autonomous replan: require an evidence-linked closed vision plus a durably settled successor frontier or terminal Goal, deduplicate by closed-vision/frontier identity, and reject ordinary completion plus blocker, stall, long-chain, and monitor replans. Keep generation, publication, and announcement as separate authority layers. |
| `periodic_report_post_writeback_hook_authority_gap` | A periodic-report producer can be called manually, or generic quota/readback code imports report policy, but a committed writeback does not produce a replay-safe typed intent through the capability lifecycle. | Primary durable receipt and settlement identity, composition-root activation, TS hook registration/input/result/sidecar receipts, provider invocation count on replay, intent write scope, and external-effect count. | Capability policy and generic control-plane lifecycle were coupled, or the automatic path relied on a best-effort diagnostic event without a typed post-writeback checkpoint. | Keep registration, admission, receipt validation, deterministic ordering, failure isolation, and sidecar replay in the core-owned TS hook boundary; keep stage policy and trigger evaluation in the capability; register only from an explicitly enabled profile at the composition root; emit an idempotent effect-free intent and require separate governed generation and publication authority. |
| `periodic_report_terminal_closeout_hook_gap` | Quota or status later proves validated no-follow-up, but no periodic-report candidate exists and monitor heartbeats remain quiet. | Last material refresh, final Todo completion receipt and settlement identity, post-writeback hook event kinds, terminal frontier projection, sidecar receipts, and external-effect count. | The hook ran only on the pre-completion refresh; the durable Todo completion transaction formed the terminal frontier afterward but did not dispatch post-writeback capability hooks. | Admit durable `todo_complete` as a typed post-writeback event, dispatch only after completion validation, persistence, and settlement readback succeed, rebuild the final frontier from committed state, and preserve replay-safe effect-free intent receipts. Keep ordinary completion and watch-only monitoring insufficient without validated stage closure. |
| `pending_capability_intent_projection_gap` | A durable post-writeback sidecar says `intent_recorded`, but quota remains `monitor_quiet_skip` or `terminal_no_followup`; no governed executor wakes, so no local artifact or exact approval Todo appears. | Exact Goal/Agent sidecar, intent schema and authority, consumption receipt, quota interaction contract, generated-artifact count, approval-Todo count, and external-effect count. | The producer journal was durable but no read model projected unconsumed capability intents into quota arbitration. Terminal or quiet lifecycle state therefore hid required capability work forever. | Add a provider-neutral TypeScript-validated pending-intent interaction slot, let the opted-in capability read only exact eligible sidecars, and give the pending action precedence over quiet/terminal routes. Consume it through an idempotent local executor that freezes checked artifacts and one digest-bound user gate; keep external delivery unauthorized and suppress consumed intents on replay. |
| `approved_capability_delivery_successor_gap` | A governed report or other capability payload is frozen and its exact user gate is approved, but quota returns quiet terminal/monitor state and no provider action runs. A later caller may also improvise a destination or default sender because the approval receipt contains no executable route. | Frozen generation/consumption receipt, completed gate decision and scope, linked agent Todo, required decision scopes before/after approval, Goal Channel binding, selected Todo, provider effect and exact sender/destination readback. | The consumer created only a user gate. Approval recorded authority but had no explicitly linked, typed agent successor for Todo/quota selection, so the external effect existed only in chat memory or caller convention. | Before the gate, create one blocked agent successor bound to the frozen generation, exact decision scope, provider capability, and safe write scope; link the gate with `unblocks_todo_id`. Approval must atomically consume only that scope and resume the successor. The provider then resolves route and sender from the durable project binding, rejects caller overrides/default fallbacks, and records success only after native effect readback. |
| `turn_replay_recovery_semantic_gap` | A real Turn resumes from a safe Journal prefix, but `inspect-journal` presents `replay_legal=false` as if recovery were forbidden; scheduler-only or saved-Host-result recovery is especially misleading. | Journal integrity fields, replay decision, executor-adopted recovery decision, completed phase prefix, complete typed settlement identity, authoritative selected-Todo lineage, conditional Host Session Binding check, prepared-effect presence, and bounded recovery outcome. | Effect-free terminal replay and effectful executor recovery were collapsed into one user-facing decision even though the executor used separate status/phase rules; initial repairs also omitted either the settlement identity or its binding-to-Turn cross-check, allowing a drifted goal, agent, or canonical Todo to reach a later effect. | Keep replay legality and Journal consistency distinct. Make one typed recovery decision the source used by both executor and inspection; validate and cross-bind the canonical settlement goal, agent, Turn instance, binding, and effect id before authorization, including the selected Todo or adaptive primary Todo; project continue/resume phase/Host reinvocation/reason plus only participating checks, and persist a bounded planned-versus-actual audit. Cover identity and binding drift with zero-provider-call regressions. Preserve the existing prepared-effect readback owner and do not claim general exactly-once semantics. |
| `managed_chat_resume_turn_identity_gap` | A managed Chat Session resumes after its adapter becomes unhealthy, returns to `ready`, and clears `active_turn_id`, but the interrupted Turn remains nonterminal and unowned. | Pre-resume Session snapshot, post-prepare persisted Session, adapter health, interrupted Turn status and error code, final Session state. | Resume preparation cleared the persisted active Turn reference before adapter recovery, then recovery re-read only the mutated Session and lost the pre-resume Turn identity. | Carry the pre-mutation Turn id through the fresh closed-state check, terminalize that Turn as `failed/server_restarted` before restoring the Session, and cover unhealthy-adapter resume with a focused regression while retaining the per-Session lifecycle lock. |
| `dashboard_chat_stdio_encoding_gap` | Dashboard Chat completes ASCII turns but Codex app-server closes on non-ASCII input on Windows; direct stderr reports invalid UTF-8. | Failing multilingual request, app-server stderr, Python locale encoding, app-server `Popen` text-pipe options, and the same request under an explicit UTF-8 control. | The text-mode stdio pipe inherited the Windows system code page even though the app-server JSON-RPC transport requires UTF-8. | Set `encoding="utf-8"` explicitly on the app-server subprocess pipes and retain a focused launch-contract regression so host locale cannot change the protocol encoding. |
| `provider_capacity_terminalization_gap` | A model provider reports temporary capacity exhaustion or overload, but the adapter stores only a generic nonzero exit and every failed Turn routes directly to manual repair; repeated restarts may also retry without a durable limit. | Provider error event and stderr adapters, public failure category, Turn Journal attempt count, recovery decision checks, controller disposition, quota/writeback receipts, and selected model before and after recovery. | Structured provider codes, model capacity, server overload, timed rate limits, and hard plan/billing quota exhaustion were collapsed into prose and then into one generic repair or retry result. | Prefer exact structured codes at the adapter boundary, fail closed on an unknown present code, and use bounded diagnostic text only when no code exists. Keep model capacity, provider overload, timed rate limit or code-less HTTP 429, and non-retryable hard quota distinct; a specific quota code must win over HTTP 429. Persist the attempt before Host invocation, attach same-configuration bounded retry only to transient classes, let the TypeScript Journal decision reject forged or exhausted budgets, and project an effect-free controller wait with explicit backoff. Preserve Session Binding when an opaque session was observed, spend no quota on failure, never switch models implicitly, and cover real protocol envelope shapes, precedence conflicts, replay, exhaustion, and private-text non-persistence. |
| `host_plugin_async_bootstrap_readiness_gap` | A host plugin is installed and its row activates, but the first user-visible command or skill catalog arrives before an asynchronous CLI or skill bootstrap finishes; manual retry or restart appears necessary even though the eventual files are correct. | Packed plugin manifest and lifecycle patch, host loader settlement contract, startup service graph, bounded CLI invocation order, generated skill files, and the first host-native skill catalog readback. | Package installation, plugin-row activation, asynchronous bootstrap completion, and host-visible readiness were treated as one event; an awaited function in a unit harness was mistaken for a host startup barrier. | Publish a typed readiness service only after bootstrap success or safe failure, make the user-facing host rows depend on that service, and keep an explicit repair command registered synchronously. On success require install plus readback before the host advertises readiness; on failure release the host without raw diagnostics and retain repair. Cover a fresh packed-profile start, first catalog readback, no-restart hot load, failure isolation, and no extra idle calls. |
| `host_plugin_system_python_mutation_gap` | A plugin bootstrap succeeds on a developer machine but a clean Debian or similarly managed host rejects `pip install` with an externally-managed-environment error, so the host starts without the promised skill. | Packed plugin startup in a clean image, selected Python version and fixed pip argv, managed runtime files, readiness settlement, first host-native skill catalog, and downstream Driver/GoalBar command resolution. | Python version availability was treated as permission to mutate that interpreter's system environment; unit fakes validated argv flow but not the host package-management boundary. | Install into a plugin-owned `--target` directory, invoke it through a managed launcher, and make every plugin face resolve that same runtime. Never use `--break-system-packages`. Retain a packed clean-container smoke that proves startup readiness and first catalog visibility without model credentials. |
| `pip_target_data_file_metadata_gap` | A wheel installed with standard `pip --target` contains its packaged resources under `<target>/share`, yet its own runtime reports those resources missing; the same wheel may work under another installer. | Wheel archive, target tree, installed `RECORD`, `importlib.metadata.files`, `distribution.locate_file("")`, typed resource-source readback, and installer identity. | pip rewrote wheel data files into the target but retained parent-relative RECORD rows that `importlib.metadata.files` filtered, while discovery trusted metadata enumeration as the only authority. | Keep metadata discovery for normal installs, then validate the fixed distribution-root resource path as a bounded fallback. Require the complete expected resource set before accepting it; cover a fake filtered metadata projection plus a real pip-target packed smoke. |
| `deep_doctor_install_contract_mismatch` | `doctor --deep` passes runtime semantics for an installed wheel but still fails a source-wrapper path, source-root identity check, or ownership check against another LoopX found on PATH. | Doctor install kind, actual invocation, PATH-default command, imported distribution ownership, wheel `RECORD` console-script row, representative installed module paths, source snapshot checks, and a conflicting-PATH clean-wheel deep-doctor run. | Deep validation reused the release-snapshot promotion contract for every installation shape, then initial wheel repair also treated the PATH-default command as the active distribution invocation. | Select the deep-validation contract by proven install kind. Keep source snapshot promotion checks and PATH diagnostics unchanged; for a Python distribution, match the actual invocation to the distribution-recorded console script and validate only packaged paths. Recognize native console-script names, retain the cross-install negative check, and cover conflicting PATH in a clean-wheel `doctor --deep` release smoke. |
| `settlement_identity_failure_phase_leak` | Settlement identity validation fails, but later unguarded poll/writeback/spend facts still produce a terminal monitor or replay phase and suppress required work. | Exact guard binding and effect id, requested identity, identity failure, later same-turn facts, projected monitor/replay phase, and work-lane obligation. | An aggregate read transaction treated the requested identity as phase authority after the guard-owned identity had already failed validation. | Gate every decision-driving phase and durable fact projection on the successfully validated identity. On missing or mismatched guard, return null phases and no unverified settlement facts; cover wrong/missing guard plus an otherwise complete later fact chain with a negative regression. |
| `receipt_bound_monitor_settlement_replay_gap` | A same-turn monitor replay remains `settlement_pending` and `must_attempt=true` after the exact poll, accountable refresh, and quota-spend receipts all exist; a quiet unchanged poll may also request settlement work it does not need. | Heartbeat receipt identity, exact monitor-poll row, `material_change`, matching refresh and spend receipts, replayed work-lane phase, and next-turn successor selection. | Receipt binding preserved the original monitor Todo but inferred phase only from whether the monitor was still due, so durable settlement receipts never closed the same-turn state machine. | Resolve the phase from exact typed receipts: no poll keeps ordinary due selection, an unchanged poll is settled by itself, and a material poll is settled only after matching writeback plus spend. Project a typed settled quiet turn with no repoll or spend, keep the automation active, and select successors only under a fresh turn identity. |
| `same_turn_due_monitor_selection_binding_gap` | A heartbeat commits an unbound pre-due receipt, the monitor becomes due in the same Turn, and explicitly selecting that exact due Todo returns `heartbeat_receipt.status=write_failed`; monitor-poll then cannot settle because the receipt is still unbound. | Original Turn receipt, requested and freshly selected Todo ids, due-monitor/capability projection, execution and interaction obligations, receipt events, and unchanged monitor-poll replay. | Explicit selection qualification recognized only `pending_action_selection`. A newly due monitor was already the authoritative automatic selection, so it lacked that marker and its exact id was misclassified as a stale competing choice. | Allow the exact currently selected capability-ready required obligation to bind the identity-less receipt after full live-decision arbitration, while a different stale request still fails closed. Project the committed selection as `heartbeat_receipt`, settle an unchanged poll with no spend, and prove replay idempotency. |
| `opaque_revision_observation_gap` | An event-gated read model returns a business snapshot and opaque source revision that each look valid, yet a source change during the read is never refreshed; runtime-only events may also consume the cursor past an unchecked business candidate. | Pre/post source revisions, exact Session cursor, candidate and runtime event order, returned snapshot, expected binding, and business-reader call counts. | The snapshot and revision were captured in different source generations, or runtime delivery advanced the cursor before source reconciliation. | Capture the binding-scoped revision before and after the business read and publish only a stable pair; retry or fail closed on drift. Reconcile source candidates before runtime-only delivery, bind the exact expected identity into the revision, reject cursors beyond the exact Session log, and cover mid-read replacement plus simultaneous candidate/runtime races. |
| `task_lease_generation_aba` | A lease is released or expires, the same todo is acquired again, and an older execution key/version can renew, release, or complete the new owner generation; a remote provider may show healthy monotonic storage generations while the LoopX fence repeats. | First and second acquire payloads, terminal lease record, full `(idempotency_key, version, lease_epoch)` fences, stale writeback result, provider generation, and release/re-acquire crash boundary. | Terminal release deleted the only per-todo generation record, so acquire rebuilt version 1; key-only mutation also made the CAS version optional and let execution-key reuse bypass no-ABA. | Keep provider generation opaque. Under the existing per-goal lock, atomically retain one inactive terminal lease record; advance authority-owned `lease_epoch` only for a new ownership generation and keep CAS `version` monotonic across every mutation. Require the current key/version pair for renew, transfer, release, and terminal writeback, reject immediate reuse of a retired key, and cover local CLI plus a live provider chain. |
| `task_session_projection_gap` | The UI says work is running while the linked Session has no active Turn, or a monitor opens an unrelated task Session. | Goal state, Todo id, Session `channel_id`, active Turn, and typed-action receipt resource ids. | Generic Goal activity was counted as active execution, or the UI joined Sessions by Goal id without the Todo id. | Count execution only from an active Turn, join task Sessions by Todo id, and create or resume a dedicated `task.<todo_id>` Session before an immediate monitor run. Cover idle resumable Sessions and multiple Todos under one Goal. |
| `chat_turn_terminal_transition_race` | A completed Chat Turn is later marked interrupted, contradictory terminal events coexist, a queued interrupt reaches another active Turn, or managed/attached terminal status appears before transcript and Session closeout. | Turn status and response, terminal event order, transcript messages, active Turn id, adapter interrupt target, completion mode, and in-memory cancellation state. | Completion and interruption read stale state before independently writing terminal status and side effects, or one completion mode bypasses the shared nonterminal closeout boundary. | Compare-and-set each Turn transition under the existing Turn file lock, scope adapter interruption to the active Turn, reserve every completion mode in a nonterminal state, replay mode-specific closeout idempotently, and publish terminal status last. Cover completion-wins, queued cancellation, managed and attached terminal visibility, and restart recovery deterministically. |
| `attached_session_expired_queue_close_gap` | An idle attached Session cannot close because an expired queued Turn remains stored as `queued`, even though that Turn is no longer eligible for dispatch. | Session status and active Turn id, queued Turn `expires_at` and status, close error code, and Turn event history. | Queue expiry was settled only by the claim path, while close treated every persisted queued row as live work. | Under the existing Session and file locks, share one expiry-settlement rule between claim and close. Persist expired Turns as `timed_out` with `session_queue_expired`, emit `turn.failed`, reject close only for live queued Turns, and cover both live and expired queues. |
| `author_owned_blocking_review_unrepresentable` | An author-owned PR receives a complete exact-head blocking review, but GitHub rejects self-`REQUEST_CHANGES` and the PR queue continues to classify the `COMMENTED` fallback as missing or malformed. | Published review state/body/title, exact review commit, machine English verdict, five required Chinese headings, and `review_conclusion.invalid_reasons`. | The review contract defined an author-owned no-blocker fallback only, so the GitHub identity restriction made a blocking conclusion impossible to encode; loose English verdict prose could also disagree with formal state. | Define separate exact `COMMENTED` titles for author-owned approval and request-changes outcomes, require a line-starting typed English verdict, validate title/verdict/state alignment, read the rendered review back at the exact head, and cover approval, blocker, untitled, and mismatched-state cases. |
| `state_projection_gap` | `quota should-run` says `should_run=true` or `must_attempt=true`, but user and agent open todo counts are both zero while `Next Action` still contains executable work. | quota payload, active state `## Agent Todo`, `## User Todo`, `Next Action`, `refresh-state` warnings. | Active state prose has drifted away from machine todo projection. | Expand executable prose into `- [ ]` agent todos, add refresh-state warning/smoke if projection missed it. |
| `deferred_todo_archive_projection_gap` | A deferred Todo appears under `Completed Work Archive`; `todo list` still projects it as deferred, but lifecycle update or resume reports that the Todo does not exist. | Active Todo and archive sections, marker plus explicit status, archive command receipt, `done_count` and `deferred_count`, lifecycle lookup result. | Completed-work compaction used the shared terminal/done projection, which intentionally includes deferred Todos, instead of selecting explicit `status=done`. | Make archive warnings subtract deferred from terminal counts, move only explicit done blocks, and prove a deferred Todo remains in the active section and lifecycle-addressable after archive compaction. |
| `selected_todo_continuation_projection_gap` | Quota selects the correct runnable Todo, but a later turn repeats completed implementation or follows a stale global `Next Action` instead of the Todo's latest bounded continuation. | `selected_todo`, selected Todo metadata, active-state `Next Action`, evaluator feedback, next-turn tool trace. | Compact Todo projections retained identity and routing fields but discarded the current note before selection, or kept stale durable action prose beside the selected Todo on the hot path. | Project one bounded, secret-screened `continuation_hint` from the selected open advancement Todo's current note or explicit suggested action. When that selection supersedes durable action prose, omit the shadowed prose from the default CLI packet and retain it behind the explicit detail path; do not copy raw evidence or require every intermediate step to write state. |
| `guided_start_turn_identity_projection_gap` | A guided-start quota guard correctly requires explicit Todo selection, but its generated `--todo-id` follow-up is rejected for missing `--turn-instance-id`, or later writeback and settlement fall back to a different Turn placeholder. | Guided transaction quota command, first `heartbeat_receipt.turn_instance_id`, returned typed selection command, upgraded receipt identity, and settlement-plan identity/commands. | The guided-start path rendered a second simplified quota command without creating a Turn, while the Codex App settlement adapter discarded a concrete runtime Turn id and rebuilt the heartbeat-template placeholder. | Make the initial guided Codex App guard explicitly begin one Turn at execution, return its minted public-safe id through the existing receipt and selection contract, and forward that exact id into every settlement step. Keep explicit selection fail-closed: `--todo-id` must reuse the returned id, never mint another one. Cover the generated command, returned selection action, direct single-Todo path, and stale/missing-id rejection. |
| `user_todo_projection_gap` | `interaction_contract.user_channel.action_required=true` or open user todos exist, but heartbeat/final message only says "owner gate" or does not name the concrete decision. | quota payload `interaction_contract`, `user_todo_summary`, active state user todos, recent final messages. | User-channel projection or agent final-response rule is too vague. | Fix prompt/projection so required user todo/question is surfaced; only say "具体 user todo 未投影" when action is required but absent. |
| `lark_inbox_priority_projection_gap` | A configured bot has a pending direct question or mention, but quota/status exposes only a static drain command and ordinary monitor/advancement work runs first. | local-private inbox counts, content-free oldest-pending age, `goal_boundary.capabilities.lark_event_inbox`, `work_lane_contract`, source-thread reply/readback receipt. | Inbox availability was modeled as a prompt reminder instead of a schedulable urgency read model and priority lane. | Project content-free pending/question/mention counts plus oldest age; make `reply_due` preempt ordinary work, then require durable effect, exactly one idempotent source-thread reply with readback, and ACK. Never project message bodies, ids, senders, or private paths into quota/status. |
| `lark_collector_profile_binding_gap` | A `configured_chat_all` inbox promises complete chat capture, but only some messages arrive or the collector and reply paths observe different bot apps; a non-mention present in bounded source history never reaches drain. | ignored collector/inbox config shape, generated service argv with values redacted, effective Lark CLI profile identity, inbox counts, bounded source-history presence, and exactly-once drain/ACK receipt. | The host collector invoked `lark-cli event consume` through the machine default profile while the reply path used a registered project profile; chat filtering and urgency classification were correct but applied to the wrong event stream. | Require an explicit non-default collector profile, or reuse the enabled reply sender profile; reject collector/reply mismatch, place `--profile` before every live collector event command, expose only content-free binding provenance, and cover non-mention ingest plus restart-safe dedupe/ACK. |
| `lark_goal_topic_pre_ingest_drop` | The provider listener observes an event, but the Goal Inbox count does not increase; listener status says only `ignored`, while the Inbox may still claim complete-thread coverage. Tokenized Bot mentions can be missed even though the binding is otherwise healthy. | Effective Goal binding and Inbox capture scopes, verified App id and Bot open-id presence, content-free listener event status/reason, Inbox counts, and one real direct-mention reply/readback/ACK receipt. | Mention routing compared the provider token only with the App id or display name, not the Bot open id; rejected events lost their reason before health writeback; and Inbox config was hard-coded to complete-chat capture instead of inheriting the effective provider scope. | Bind and compare both verified App id and Bot open id, preserve a small content-free rejection reason enum through listener health, and make persisted Inbox scope equal effective provider scope. Never label addressed-only capture thread-complete, while still allowing an exact source-message reply. Require explicit configured-chat-all selection for complete-thread semantics and cover both modes plus one real provider event. |
| `lark_bot_history_permission_guidance_gap` | Bot event delivery works, but bounded group-history pagination returns provider code `230027`; UI reports only generic message access or suggests realtime-event/user-auth scopes, and the operator may edit the wrong App. | Exact Bot list-messages call, identity, CLI stdout and stderr envelopes, nested provider error code, selected App id, and app-bound official API page. | Realtime receive, exact message hydration, Bot send, and history catch-up were collapsed into one permission state; the command wrapper parsed only stdout/top-level codes; repair guidance omitted the selected App binding. | Model Bot history catch-up as a distinct capability. Parse bounded JSON errors from stdout or stderr and top-level or nested error envelopes, but never infer the failed capability from provider code alone. The history preflight projects the exact application scopes `im:message.group_msg` and `im:message.group_msg.include_bot:read` plus an app-bound official API link. Require publish and real Bot pagination verification; never redirect this repair to user OAuth or reuse it as exact-message repair guidance. |
| `reviewer_reader_auth_misclassified_as_identity_drift` | A reviewer sink has explicit user-reader and bot-sender bindings, the sender bot remains healthy, but an unavailable reader user credential is reported as an identity mismatch and the agent starts rewriting profiles instead of requesting login. | Goal-default sink shape with profile values redacted, content-free `auth status --verify` availability for the configured reader and sender, provider call count, and secondary notification blocker. | Runtime user authentication availability and static profile-binding validity were collapsed into one identity-mismatch blocker. | Preserve the two explicit bindings; return a content-free reader-auth-required blocker, project the existing scoped login action, and perform zero destination reads or sends until user authentication verifies. Cover unavailable reader auth with a focused sink smoke. |
| `normal_no_user_todo_misflagged` | `action_required=false` and user open count is zero, but the agent reports a state projection fault. | quota payload and user todo summary. | Rule over-applied the "todo not projected" fallback. | Allow "无用户待办/无需通知"; update heartbeat/prompt smoke if needed. |
| `todo_no_followup_global_terminal_overreach` | A completed scoped Todo records `done + no_followup`, but `refresh-state --repair-delta-kind no_followup` becomes `replan_noop` because unrelated advancement or long-running monitor Todos remain open. | rejected repair-delta claim, complete Agent Todo projection, scoped Todo metadata, and the latest local no-follow-up completion. | The repair-delta validator reused Goal-level terminal-frontier completeness to validate a Todo-local closure. | Keep Goal `terminal_no_followup` strict, but validate repair-delta no-follow-up against the latest scoped completed Todo in a complete parser projection; reject other-agent, continuation-required, malformed, or truncated evidence and keep the emitted Todo evidence bounded. Cover the real CLI path with an open monitor regression. |
| `terminal_settlement_ordering_gap` | Final Todo completion succeeds, then the matching refresh or spend is rejected because the Goal is already terminal; different hosts may expose different closeout orders or retry the material effect. | Typed settlement plan and effect identity, Todo completion event, accountable refresh and spend receipts, Turn journal phase prefix, and host task-body projection. | Final `no_followup` was modeled like ordinary pre-settlement Todo completion even though it mutates Goal terminal state and activates the strict no-more-spend guard. | Keep Goal terminal guards strict. Treat ordinary successor completion as Todo lifecycle work, but make final `no_followup` a conditional post-spend terminal closeout under the same effect identity. Require matching writeback and spend receipts at the mutation boundary, prove the closeout event itself carries `no_followup`, and journal it so closeout-only retry cannot repeat writeback or spend. Cover CLI, visible Goal, Codex App, Turn driver, adapter order, failure short-circuit, and replay. |
| `host_settlement_source_projection_gap` | A visible Goal host packet requires `source=visible-goal` without an explicit Turn receipt, but the nested interaction contract tells the agent to spend as `heartbeat` and binds the currently selected Todo. The command can fail after Todo completion or misattribute interactive usage. | Host runtime profile, canonical host quota commands, interaction-contract CLI actions, selected Todo lifecycle order, and the failed spend preview. | The generic interaction projection hard-coded heartbeat accounting and selected-Todo binding instead of deriving the accounting route from the scheduler execution context. | Route spend actions from the typed runtime profile. Native Goal runtimes preserve `source=visible-goal` and host-level attribution; heartbeat automation keeps its Todo- and Turn-bound settlement plan. Cover the exact CLI actions for each native Goal profile and retain the heartbeat regression. |
| `visible_goal_settlement_replan_attribution_gap` | An accountable visible-Goal delivery writeback moves the frontier into capability or control-plane repair; the following unbound spend is recorded against the new repair state with no Todo, Turn, effect, or delivery-workspace causality, and an exact retry cannot replay the committed effect. | Newest same-Agent accountable run, its complete nested settlement identity, top-level Todo/Turn fields, original heartbeat receipt, delivery-workspace causality, current quota frontier, spend classification, and retry run count. | The unbound visible-Goal path used the mutable current frontier as settlement authority and did not recover the completed delivery identity; commit classification also represented eligible, repair, and delivery semantics as overlapping booleans, allowing a later repair projection to overwrite attribution. | Allow unbound recovery only for visible-Goal spend from the newest fully typed same-Agent accountable run; require its top-level and nested identities to match the TypeScript-generated settlement payload and revalidate the original heartbeat receipt. Preserve Todo/Turn/effect/workspace causality, write the CLI receipt under the recovered Turn, and reduce commit semantics to one closed TypeScript disposition where proven delivery completion precedes later frontier repair. Cover missing/legacy identity, capability and control-plane replan, and exact one-run replay. |
| `runtime_capability_user_gate_overreach` | A missing launcher/runtime capability such as `network` becomes `capability_gate.action=ask_owner`, `requires_user_action=true`, or a user notification even though no credential, protected access, or production authority is needed. | quota `capability_gate`, heartbeat `--available-capability` inputs, launcher/runtime observations, interaction contract user channel. | Capability availability and permission authority were collapsed into one owner-held class. | Keep runtime provisioning gaps in the agent repair lane, require truthful runtime observation before declaring availability, and reserve `ask_owner` for credentials or protected/production authority. Cover all-blocked and mixed-capability cases with capability-gate and monitor smokes. |
| `visible_goal_dynamic_capability_projection_gap` | A visible Goal session proves a runtime capability such as `network` at a real callsite, but later continuations keep running the activation-time quota command without that capability and repeatedly enter `capability_bridge_repair`; a bounded host tool result truncates before the re-entry packet; or a same-turn re-entry selects a newly runnable Todo and conflicts with the heartbeat receipt already bound to the admitted Todo. | Activation-time capability list, later real-callsite observation, complete versus host-captured quota byte counts, byte offset of `runtime_capability_reentry_v0`, quota guard and follow-up argv, capability-gate result, heartbeat receipt settlement identity, and re-entry selected Todo. | The CLI decision surface lacked a typed re-entry packet, emitted it after diagnostics larger than the host capture budget, or recomputed Todo priority without treating the committed same-turn settlement identity as an immutable selection binding. | Keep the Goal prompt stable. Return the real-callsite-gated re-entry packet near the beginning of JSON CLI output; after verified re-entry, preserve observed capabilities in generated follow-up actions and prefer the receipt-bound Todo when it remains runnable. Fail closed on an unavailable bound Todo or an explicit conflicting Todo request. Never persist observations as grants. Cover blocked and bounded-capture packets, successful re-entry inheritance, owner-capability exclusion, receipt-bound replay, and explicit conflict. |
| `blocked_priority_fallback_hidden` | A core P0 path is user-gated, safe P1/P2 fallback runs, but the user is not told what blocked the P0 path. | quota `safe_bypass_allowed`, active priority stack, final heartbeat text. | Fallback execution hid the higher-priority blocked item. | Report concrete blocker plus fallback progress; expose `blocked_priority_fallback` in state/payload if missing. |
| `boundary_projection_gap` | `recommended_action` asks for work in a scope that is absent from `goal_boundary.write_scope`, such as runner edits blocked by repo-only scope. | quota `recommended_action`, `goal_boundary.write_scope`, authority/owner decision records. | Checkpointed owner decision did not project into current write boundary. | Repair boundary projection or ask controller/user; do not consume repo-only handoff steps that cannot touch the needed scope. |
| `public_surface_internal_context_leak` | Public docs, examples, fixtures, catalogs, README links, PR descriptions, or shipped evidence contain internal planning labels, meeting/reporting context, local-only status narratives, private operator framing, raw coordination details, or references to ignored local artifacts. | `git diff --stat`, `git diff --name-only`, `git grep`/`rg` over tracked public surfaces, docs indexes, fixture schema names, PR refs/history if already pushed, `.gitignore`, `.local` target path. | The agent treated a public artifact as durable memory for private planning, or generalized too late after internal context had already entered tracked history. | Move private planning to `.local` or another ignored state store, rewrite public text into generic product/maintainer language only when it is truly reusable, scan current public heads and PR refs, add a user/support gate if pushed read-only refs or cached views still expose the context, and record a concrete prevention todo before resuming feature work. |
| `periodic_report_editorial_contract_gap` | A periodic-report artifact claims operational context is collapsed, but the first real hosted report puts normalized-document parity, renderer/digest details, delivery receipts, archive-provider canaries, or other process narration in the hero or primary sections; a later readability pass may instead delete primary facts, omit supporting facts from copied text, or leave controls untranslated even when the profile language is set. A hosted shell may also expose supporting metadata or fail direct section hashes after dynamic mount. | Normalized document item kinds and visibility, compiled editorial lineage, primary and supporting item counts, original-to-rendered fact checklist, renderer `content_policy`, Markdown/HTML completeness, localized hosted DOM first viewport, initial hash position, source snapshot refs/digests. | Audience versus operational content was a prose convention instead of a normalized document contract, and readability was treated as deletion rather than information hierarchy; an authored hero summary could bypass item visibility, and language metadata did not own the built-in UI vocabulary. The host may also re-render the document without preserving renderer visibility semantics. | Compile hero summaries only from typed primary outcomes, risks, and next actions, record exact item/field lineage, and make renderers reject authored or stale summaries; preserve audience facts through bounded named detail rows; require runtime and delivery-receipt items to be supporting; collapse them in HTML and retain them in a labeled Markdown appendix; localize built-in controls from the profile language; validate source-to-artifact counts and representative facts; publish HTML artifacts without re-rendering or validate presentation parity; restore section hashes after mount; treat mutable source links as navigation rather than frozen evidence. |
| `dashboard_visual_acceptance_gap` | A dashboard passes document-level horizontal-overflow checks and its first viewport looks correct, but narrow cards or fixed-column tables clip internally, mutable text makes the same stable Todo appear twice, or a requested live source briefly renders synthetic example data. | Dual-size screenshots after live data settles, per-panel `scrollWidth` versus `clientWidth`, rendered Todo keys grouped by goal/role/stable id, and the initial loading/error DOM before a live response succeeds. | Visual acceptance checked only the document root, Todo identity included mutable display text, and the initial payload was treated as valid content while an explicit source was still loading. | Use responsive panel columns sized for their widest durable content; assert internal overflow on named high-risk panels; deduplicate by goal, role, and stable Todo id while retaining historical-only records; and render an explicit loading/error state until the requested live source succeeds. Cover the real long-evidence and same-id/different-text cases in a browser smoke. |
| `dashboard_idle_port_timeout_misclassification` | On native Windows, `loopx dashboard` reports that every unused loopback port belongs to a foreign service and exits before binding, while the listener table shows no owner and another local server can bind the same port. | JSON dashboard startup error, listener-table readback, the exception raised by the capabilities HTTP probe, and a successful bind/readback on the same loopback interface. | The launcher recognized only `ConnectionRefusedError` as an unused port; a Windows host or execution boundary returned `TimeoutError` for the same no-listener state, which the broad `OSError` branch classified as a foreign service. | Treat connection refusal and probe timeout as non-reusable, then let the later server bind remain authoritative for actual port ownership. Preserve exact matching/stale/foreign capability checks, and cover the timeout classification plus packaged dashboard startup path. |
| `dashboard_status_hot_path_audit_gap` | The dashboard remains on its initial loading state for several seconds even though it has only a few Goals, and changing the status display limit has little effect. | Browser time-to-first-workspace, direct `/status.json` timing, `collect_status` profile, scanned-file count, regex-call count, and Git subprocess count. | The status read model runs repository-wide publication or private-boundary audits synchronously on every first-screen request. | Keep Goal, Todo, Agent, and runtime projections on the status hot path; defer the repository-wide public-boundary scan with an explicit machine-readable state; preserve the complete scan in `loopx check`; and cover both routes with focused tests plus a real status-endpoint timing probe. |
| `content_layout_sequence_acceptance_gap` | A carousel passes independent per-page density checks, but the cover is not the density peak or interior pages remain visibly sparse. | Typed layout plan, selected template `page_sequence`, renderer measurement packet, per-page densities, and `layout-check` failures. | Acceptance modeled each page in isolation and omitted sequence-level cover prominence and interior compactness defaults. | Keep project-specific narrative rules in the writing adapter, but make built-in templates require a cover first, a first-page density maximum, and an interior density floor; lock low-interior and denser-than-cover cases with focused negative smokes. |
| `shared_adapter_local_path_leak` | A shared adapter projection, such as Lark Kanban sync, would write an absolute local checkout, runtime, active-state, raw-log, or artifact path into a remote/shared row. | adapter dry-run payload, generated record values, docs boundary section, public/private scan. | Adapter reused local executor context as shared control-plane metadata. | Strip or replace the local path at the adapter projection layer, add a focused smoke that rejects the path, and only store explicit worker launch context when the row was authored for that purpose. |
| `peer_workspace_boundary_gap` | A peer edits or prepares repository changes from a non-git directory, unrelated worktree, or checkout that violates the selected task/repository isolation policy; a cross-repository repair may also be misclassified against the goal repo. | `agent_identity`, selected todo `task_repository` and write scopes, goal `repo`, current git origin/worktree/root, quota payload, heartbeat prompt commands, AGENTS worktree policy. | Workspace safety was inferred from identity labels, prompt text, or the goal repo alone instead of the selected task and `quota should-run`. | Add or repair `agent_workspace_guard_v1`; use a credential-free first-class `task_repository` only for genuine cross-repository tasks, and require a matching linked worktree. Block delivery and spend until the peer moves to a compliant worktree and reruns the guard with the same `--agent-id`. Keep the goal repo fallback and do not treat the field as scope or write authority. Read-only and monitor-only work should not be blocked merely by identity. |
| `workspace_guard_action_selection_projection_gap` | A turn starts in a worktree that matches one eligible Todo but not the default Todo; quota emits `workspace_guard`, commits an identity-less receipt, omits the action portfolio, and rejects same-turn selection of the matching Todo as unprojected. | Initial and selected `quota should-run` packets, selected Todo repositories, workspace guard, action portfolio, interaction selection command, and heartbeat receipt identity. | The workspace repair route correctly disabled default delivery, but action-portfolio projection incorrectly treated that route as if no eligible alternative could be selected. | Keep the workspace guard fail-closed for the default Todo, but preserve an identity-less action portfolio when ordinary delivery was otherwise eligible. Revalidate the explicitly selected Todo and its exact workspace before binding the receipt. Cover guard-first selection and same-turn upgrade without weakening repository isolation. |
| `monitor_successor_routing_gap` | A material monitor poll creates a natural-language `advance` successor for another repository, but the child has no repository, action, continuation, capabilities, stable target, or creation receipt; quota then routes plausible work without a safe workspace contract. | Parent monitor metadata, `quota monitor-poll` argv and artifact, child Todo metadata, rollout event successor ids, selected Todo workspace guard. | The agent extended a bounded observation into standing work, while the monitor-poll successor API accepted only text and an optional claimer and discarded typed routing. | Retire the unintended monitor/vision. Require explicit action semantics for every monitor-generated agent successor and explicit successor repository when the source monitor is repository-bound; carry optional execution capabilities, default continuation to `independent_handoff`, and derive a stable transition target when none is supplied. Record compact successor ids and routing receipts in the monitor artifact and rollout event. External-repository work must re-enter through that typed Todo rather than prior observation prose. |
| `delivery_workspace_causality_projection_gap` | A validated delivery is written in one worktree, but `refresh-state` or `spend-slot` is attempted from the goal repo, a canonical checkout, or another repository; the workspace guard correctly rejects accounting and the agent must discover the right invocation by trial and error. | Latest unspent accountable run `delivery_workspace`, selected Todo `task_repository`, refresh command cwd or `--delivery-workspace-path`, spend command cwd, `workspace_guard`, and the generated `interaction_contract.cli_channel`. | The fail-closed accounting guard owns the invariant, but the machine interaction contract exposed only generic refresh/spend commands and omitted the causal workspace requirements. | Preserve the guard. For spend-capable selected Todos with an explicit `task_repository`, project compact `delivery_workspace_causality_v0`: refresh from the producing workspace or pass its explicit delivery path, then spend from the recorded accountable workspace. Omit it from default and no-spend hot paths. |
| `material_monitor_workspace_settlement_gap` | A repository-bound material monitor poll records validated progress and a durable writeback receipt, but `quota spend-slot` rejects settlement because the monitor event has no delivery workspace and the matching `refresh-state --delivery-workspace-path` is treated as an ordinary idempotent replay. | Turn-scoped settlement identity and receipts, selected Todo workspace causality, material monitor run, matching refresh-state result, and spend failure. | Material monitor writeback became accountable before refresh-state could attach the required independent-worktree snapshot, while replay handling made the first durable receipt terminal even when its workspace causality remained incomplete. | Keep ordinary receipt replay idempotent. When causality says a workspace is required, the prior writeback has none, and the caller supplies an explicit validated workspace path, let refresh-state append exactly one supplemental causal record; replay that supplement idempotently and require spend to validate the recorded repository/worktree. Cover the no-path no-op, one supplement, supplement replay, and final spend. |
| `codex_app_ssh_host_loop_misclassification` | Codex App is connected to a remote project over SSH and has no automation tool, but onboarding or quota still requires heartbeat receipts, RRULE mutation, or a local-scheduler handoff. | exact host surface and agent type, available host tools, scheduler execution context, activation packet, generated task body, quota source, and usage summary classification. | Desktop Codex App and Codex App over SSH were collapsed into one host profile even though only the former owns heartbeat automation. | Add an explicit `codex-app-ssh` agent type and `codex_app_ssh_goal` runtime profile owned by the interactive agent CLI loop; activate one visible `/goal <task_body>` within the host character budget, omit heartbeat/RRULE instructions and receipts, account the validated run with `source=visible-goal`, and do not classify that spend as automation. Cover the generated command by parsing it through the real CLI. |
| `codex_app_ssh_quiet_continuation_storm` | A visible SSH Goal reaches `monitor_quiet_skip` or another unchanged wait, but the host immediately starts another model turn, repeats the same user notice, and never reaches a bounded pause. | typed scheduler execution context, `scheduler_hint.unchanged_poll`, generated visible Goal body, consecutive quota/monitor rows, and host Goal status. | The SSH runtime was typed as `agent_cli_loop`, but scheduler limits covered only local, CLI TUI, and Claude runtimes; the prompt prohibited RRULE mutation without connecting the SSH path to Codex `/goal` blocked state. | Project a `codex_app_ssh_goal` unchanged-poll limit while sharing Codex CLI's native Goal contract. At the third unchanged blocked turn, rerun quota once and call `update_goal(status=blocked)` if unchanged; do not spend or complete LoopX. Resume only through user `/goal resume`, then rerun quota. Cover both native Goal profiles, the compact packet, and generated CLI path. |
| `goal_phase_handoff_drift` | A visible or managed Goal completes one bounded phase, then the agent starts a successor host Goal for screening, implementation, review, or another ordinary phase even though the registered objective remains active. | generated Goal task body, typed host contract, active vision and todo frontier, host Goal ids, recent run history, and the user correction. | The prompt described bounded delivery segments without distinguishing them from the host Goal lifecycle, so phase progress was misread as a new Goal boundary. | Keep one activation as owner of the registered Goal until terminal; state that a bounded segment is internal progress, project `goal_lifecycle_scope=registered_goal_until_terminal` and `phase_handoff_allowed=false`, and cover both Managed Agent and visible Goal prompt families. Use a fresh Goal only for a materially different objective or an explicit recovery/test boundary, not ordinary phase continuation. |
| `host_native_goal_command_collision` | Adapter docs or onboarding imply that LoopX shell commands, MCP tools, and a host's native slash commands share one namespace; a user expects native `/goal` or `/goal-pro` completion to update LoopX, or starts both loops for the same task. | exact host slash help, adapter launch argv, exposed MCP tool list, project binding, and both host-native and LoopX goal states. | “First-class host surface” was described without naming the separate command namespaces and lifecycle owners, or an execution mechanism such as Arrangement was overstated as a separate Goal product semantic. | Publish an explicit command matrix; state whether the adapter invokes any native Goal mode; describe native strict variants as their shared Goal lifecycle plus the exact additional completion gate, with delegation named as its enforcement mechanism; choose exactly one outer lifecycle owner per task; require an explicit result/writeback mapping before using a native Goal as an inner executor. Never infer LoopX completion from host-native terminal state. |
| `host_native_goal_activation_gap` | An adapter launches a normal headless prompt and is described as autonomous long-running Goal execution, but the host slash command is TUI-owned, no model Goal-creation tool or app-server Goal mutation is invoked, and no scheduler launches another bounded turn. | host CLI help, model tool contract, app-server Goal methods, adapter launch argv and prompt, native Goal readback, scheduler ownership, ignored runtime journal, and agent-scoped writeback evidence. | Persistent control-plane state, native host Goal continuation, and repeated external process invocation were collapsed into one “long task mode.” | Name all supported Goal activation paths and their authority; state which one the adapter actually calls; never treat a long prompt as native Goal activation. For deterministic managed native Goals, have the outer controller create/read the host Goal through its machine API, start or resume the first turn, persist only opaque thread/goal lineage plus objective digest, require the exact verifier-backed terminal event for strict mode, block model-visible MCP and same-goal CLI lifecycle writes until that proof, and reconcile delivery/todo/quota phases against scoped evidence before retrying. Keep one outer lifecycle owner. Otherwise describe each run as bounded and require an explicit external scheduler for repetition. |
| `implicit_review_route_drift` | `todo complete` invents a default reviewer, routes completion by an agent profile, or blocks the completing peer without an explicit review request. | failing `todo complete` preview, successor continuation policy, todo claim metadata, generated heartbeat prompt, todo lifecycle smoke. | Review ownership survived as an identity-level parent/default handoff instead of task policy. | Use typed continuation: ordinary `independent_handoff` is non-blocking, `same_agent_non_delivery` stays with the peer, and only explicit `review_handoff` to a different registered peer creates a blocking dependency. Profiles must not supply an implicit reviewer. |
| `nonblocking_pr_review_continuation_gap` | A validated feature PR is waiting for human review and the same agent has independent runnable work, but the source todo remains blocked or the generated user todo becomes `user_gate`, so quota stops normal delivery. | source completion command, generated user/agent todos, user todo `task_class`/binding/gate scope, PR monitor, quota interaction contract, branch base topology. | `--next-user-todo` defaulted to a blocking gate and the workflow treated review latency as merge authority; no explicit reminder-versus-gate choice was recorded. | Complete the feature slice, derive a bound `user_action` with `--next-user-task-class user_action`, keep a runnable agent successor, and add a separate `continuous_monitor` for PR lifecycle readback. Use a stable integration branch when stacking experimental feature PRs and reserve `user_gate` for the exact aggregate merge/release/launch or protected authority boundary. Cover the generated todo metadata and eligible quota path in the todo lifecycle smoke. |
| `nonblocking_review_empty_frontier_replan_gap` | A bound `user_action` remains open after a feature PR, no runnable agent successor exists, and two bounded stalls are visible, but status still reports `operator_gate` or the replan only records prose. | active-state user/agent todo summaries, `waiting_on`, run-history stall evidence, replan obligation `todo_actions`, execution/interaction contracts, refreshed repair delta. | Active-state routing required an existing agent todo before treating `user_action` as non-blocking, and empty-frontier replans did not require concrete todo writeback. | Keep pure `user_action` as a visible non-blocking notice even on an empty agent frontier. After the stall threshold, require `autonomous_replan_required` to create and claim a runnable agent todo and record `runnable_todo_set`; allow terminal no-follow-up only with closure evidence. Preserve `user_gate` precedence for exact authority boundaries. |
| `nonblocking_user_action_execution_suppression` | Quota selects a required non-delivery obligation such as a ready-successor replan while an unrelated `user_action` is open, but the final interaction contract becomes `notify,wait` and scheduler backoff blocks the Goal. | Selected Todo, execution obligation `must_attempt_work` and `delivery_allowed`, user Todo task class, final interaction channels and CLI actions, and scheduler disposition. | Empty-agent-lane detection looked only at ordinary executable Todos. It treated a selected control-plane obligation with `delivery_allowed=false` as absence of agent work, promoted the notice to a blocking user action, and discarded the selected command. | Let an open `user_action` own the empty lane only when no selected execution obligation must be attempted. Preserve the obligation mode, selected lifecycle command, no-delivery policy, and active scheduler cadence while projecting the user item as a non-blocking notice. Keep explicit `user_gate` precedence and cover successor replan plus other non-delivery obligations end to end. |
| `repeat_vision_wait_ack_gap` | Two exact blocked-successor stalls correctly trigger replan, but a maintenance-only continuation appears to close the obligation while a `repeat_until_closed` vision still has no evidence-linked path outcome; later heartbeats repeat the same wait cycle. | active agent vision advancement policy, blocked-successor frontier identity, current obligation id, host-projected coverage ledger, typed progress observation, current runnable successor ids, and semantic receipt. | Replan validity considered a caller-supplied delta label instead of the obligation's typed outcomes and current state. | Require an accepted semantic delta bound to the exact current obligation. A vision-derived duty accepts a fresh evidence-linked vision path, new evidence-backed blocker, or coverage-backed terminal; a successor id is accepted only when current state proves it runnable, and maintenance-only continuation never closes the vision duty. Cover repeat/as-needed routing and stale-obligation identities. |
| `future_monitor_heartbeat_liveness_overreach` | A valid future-scheduled monitor repeatedly enters `frontier_exhausted_monitor_lane` or `dead_monitor_repeat` even though it is not due and no real monitor execution occurred. | generated heartbeat guard command, trigger `current_time_iso`, quota `heartbeat_receipt`, monitor Todo `next_due_at`, run-history `quota_monitor_poll` rows, concrete Todo/target identity, and monitor mode. | Future wait state was collapsed into empty-frontier exhaustion, while distinct heartbeat turn ids were treated as monitor executions and lowered the dead-monitor threshold. | Add typed `future_monitor_wait` and `due_monitor_execution` frontier rules. Keep future monitors quiet/no-spend until due; count `dead_monitor_repeat` only from unchanged due/external executions carrying a concrete Todo or target identity. Heartbeat turn ids remain idempotency/liveness evidence and never reduce the real execution threshold. |
| `replan_context_delivery_gap` | A replan obligation is visible, but a weak protocol-following model ignores or mis-executes a manual evidence-log read ritual, then spends turns debugging ACK/read order instead of choosing a new direction. | current obligation id, host-projected `replan_context_v0`, compact coverage ledger, delivery receipt, typed action packet, selected model tool action, semantic writeback receipt, and successor turn boundary. | Evidence access was modeled as an agent CLI obligation even though the control plane already owned the ledger; read execution and semantic use were conflated. A separate Todo-add then refresh ACK also let the model execute the successor in the replan turn. | Have the host prefetch and project bounded evidence coverage into quota. Treat the delivery receipt as context delivery only; require the model's real next action to produce an accepted typed semantic delta bound to the current obligation. When the delta is a runnable successor, bind the exact obligation on the Todo write itself and return `end_current_heartbeat`; do not require a second ACK or run the successor in that turn. Keep evidence-log as the durable chronology and optional drill-down, not as a closeout gate. Cover real quota→model action→Todo/refresh CLI behavior plus missing/wrong obligation identity. |
| `replan_settlement_binding_projection_gap` | A replan packet and selected Todo are both visible, so an agent combines `--todo-id` with `--replan-obligation-id`, alternates between them after rejection, or performs the Todo-bound refresh but omits the matching spend. | Original quota receipt identity, selected Todo, semantic obligation id, interaction settlement plan, projected refresh/spend commands, and accepted semantic ACK. | The semantic obligation target and causal settlement identity were adjacent but unlabeled, while one boolean conflated direct obligation binding with whether a full settlement chain was required. | Keep exactly one causal settlement binding. For a turn-scoped executable plan, project a domain-local typed replan settlement contract that labels the selected Todo or Todo-less obligation as the binding and keeps the replan id as a separate semantic target when necessary. Todo-bound and directly bound replan writes both project material refresh followed by spend; never combine their CLI flags. Keep unscoped diagnostic reads compact and non-executable. Cover TS projection, hot-path envelope, real Todo-bound/Todo-less CLI chains, output budget, and in-flight delivery continuity. |
| `native_goal_todoless_replan_reentry_gap` | A native visible Goal completes all advancement Todos, semantic closeout derives a Todo-less autonomous replan obligation, and the next unbound quota read projects a bare refresh with no Turn identity or spend chain. | Native Goal runtime profile, selected Todo absence, replan obligation id, interaction CLI actions before and after Turn creation, and settlement identity. | Native-Goal Turn re-entry recognized only selected Todos as actionable settlement bindings and ignored a valid Todo-less replan obligation. | Treat either a selected Todo or a valid replan obligation as a native-Goal settlement binding. When no Turn or settlement plan exists, project only the exact `quota should-run ... --begin-turn` re-entry. After execution, require the obligation-bound refresh and spend chain; keep generic unscoped diagnostic behavior unchanged. |
| `replan_meta_successor_churn` | An autonomous replan repeatedly adds and completes obligation-bound advancement Todos whose only action is to replan, re-arm, wait for the host, or report no new direction; Todo count grows while executable targets and evidence do not. | current obligation and acceptance gaps, generated replan action packet, bound Todo `action_kind`/`target_key`/Explore refs, semantic ACK, Todo lifecycle, and compact progress fingerprints. | A planning instruction was compiled into a runnable successor command, while Todo authoring and ACK validation treated obligation identity alone as proof of executable semantic change. | Generate a successor command only from a concrete bounded frontier with a typed action and stable target. Require the same semantic binding at Todo write and ACK time; generic vision/frontier replans must resolve through a typed progress or coverage-backed terminal outcome instead of creating a meta Todo. Cover packet generation, write rejection, ACK rejection, and a real quota→model→CLI successor re-entry. |
| `global_route_collision` | A peer runs `connect`/`bootstrap` with an existing `goal_id` from a different repo or state file, and the dashboard appears to lose state even though the original files still exist. | global registry entry, `source_registry`, `repo`, `state_file`, project-local registry, active state path, recent `sync-global` or `connect` payload. | `sync-global` merged by `goal_id` and silently rewrote the global route pointer from a non-authoritative checkout. | Fail closed on route changes unless `--replace-state` is explicit and backed up; use `register-agent` through the existing `source_registry` when adding a peer, or choose a new `--fork-goal` id for a true fork. Cover with a focused connect-route-collision smoke. |
| `shared_authority_conflict_scope_drift` | Two eligible agents claim independent todos under one goal, but one receives a goal-revision or head-advanced conflict even though its target, named dependencies, gates, authorization, and write scope did not change. | public todo/lease contention contract, command semantic fields, provider generation, goal audit sequence, and paired same-todo versus independent-todo concurrency probes. | A whole-aggregate CAS token or goal audit sequence escaped the storage seam and became a shared business precondition for every command. | Keep provider generation as the physical fencing token and the goal revision as audit order. Bind commands to the target todo and named preconditions; after a CAS miss, reload, replay-check, revalidate, and internally retry when relevant facts are unchanged. Preserve state-plus-operation-result atomicity and same-todo conflicts; when write scopes enter the command contract, preserve overlap conflicts too. |
| `authority_receipt_layer_collapse` | An A-B-A retry is declared safe because the storage provider can replay publication A, or a historical provider generation is returned after B has already replaced the head. | Current aggregate bytes and generation, inherited semantic receipt index, provider publication receipt, immediate replay and A-B-A probes, and the provider's current-head replay guard. | Domain receipt, provider operation receipt, and current storage authority were treated as one identity; the implementation retried an old physical publication instead of resolving the logical operation from the current aggregate. | Keep semantic operation ids and receipts inside the atomically CASed aggregate, and use a distinct physical attempt identity for each provider write. On every retry or ambiguous result, load the current head first: return the inherited semantic receipt on an exact digest match, revalidate/rebase when it is absent, and never reuse a historical provider generation as the current CAS token. Keep domain decisions, authority execution results, and provider outcomes as separate typed layers. Cover immediate physical replay, A-B-A semantic replay, same-id mismatch, lost response, and head-advanced rejection against a real provider path. |
| `guided_goal_identity_reuse` | A new `start-goal --guided` or `agent-onboard` invocation without `--agent-id` silently reuses the first goal's only registered agent, only asks for identity when several agents exist, or forces a stable unbound host thread to take over an old lane. | guided start packet, stable host thread id and binding status, project-local goal order/status, requested versus selected `goal_id`/`agent_id`, fresh-registration/default-action fields, host-loop activation identity gate, shared global registry after `register-agent`. | Guided startup collapsed a new stable host session into existing-lane continuation, or treated registry order and single-agent convenience selection as identity evidence; requiring the user to restate “new peer” made fresh registration opt-in even though the host already supplied a distinct session boundary. | Return a machine-readable goal-selection gate until `--goal-id` is explicit. Reuse a verified binding for the same stable thread. For a stable unbound thread or other new onboarding with no active identity contract, default to a distinct public-safe `--agent-id`; treat the `register-agent --require-new` preview as advisory; require execute `ok`/`changed`/`written`, global sync, source/global readback, and binding readback before todo writeback; and require explicit exact-agent takeover intent before reusing an existing id. Without a stable thread id, fail closed unless exact `--agent-id` or explicit `--new-peer` intent is present. Keep the source-registry collision check and write in one bounded lock transaction. Preserve explicit registered ids for ongoing continuations, keep registration pinned to the shared source-registry route, and cover zero/one/many/concurrent-collision/stale-preview seams with focused tests and smokes. |
| `linked_worktree_goal_route_reuse` | A fresh `start-goal --project <worktree>` packet returns another linked worktree's project or goal merely because both share one Git common directory. | requested project, `project_connection`, `canonical_project_alias`, project-local and global registries, Git top-level and common-dir. | Explicit goal start reused the diagnostic canonical-alias policy, so an older registered worktree route overrode the requested task workspace. | Keep the exact requested project route for explicit `start-goal`; reserve canonical worktree aliasing for lower-level connection diagnostics and repair. Cover both routes with focused linked-worktree tests. |
| `global_registry_write_boundary_gap` | `connect`, `sync-global`, or agent registration can write/read project-local state, but writing the shared global registry fails with permission or read-only host errors; the user sees an ambiguous "not connected" or a half-updated control plane. | command payload `global_sync`, `global_registry_writability`, project-local registry, global registry path, `loopx doctor`, filesystem write probe, recent source registry contents. | The shared global registry is a required projection for multi-project status/quota, but command flow treated it as best-effort or discovered write denial after mutating the source registry. | Probe global registry writability before source/local mutations when global sync is required; fail fast with `ok=false`, `global_registry_write_denied`, and recovery guidance. Keep explicit local-only mode behind `--no-global-sync`, and prefer `register-agent` through `source_registry` for agent onboarding. |
| `cross_goal_health_contamination` | A registry or active-state error owned by one goal makes `status --goal-id` or `quota should-run --goal-id` unhealthy for an unrelated goal. | contract `error_diagnostics`, `global_errors`, `goal_errors`, selected-goal quota `status_health_ok`, filtered status output, and the original registry/active-state producer. | Error ownership was encoded only in message text, then aggregate `contract.ok` was reused as a per-goal execution guard. | Emit structured ownership at the validation source; keep registry identity ambiguity and registry/public-boundary failures global, attribute goal-entry and active-state todo failures to their goal or affected goal set, and derive legacy string lists from diagnostics. Per-goal decisions consume global plus selected-goal errors; broad inventory remains all-goal. Cover similar goal ids, unrelated-goal failures, shared-goal findings, and global fail-closed behavior. |
| `runtime_projection_source_mirror_ambiguity` | A project-local material refresh appends its source run, but shared-runtime projection reports `ambiguous` when the source runtime's registry mirror and exactly one external runtime both declare the same source registry. | refresh-state route diagnostics, source and candidate runtime roots, each registry's `source_registry`, compact route match counts, and an immediate shared-runtime readback. | Route discovery counted the source runtime's local registry mirror as an independent projection target alongside the authoritative external runtime. | Prefer external matches when any exist, report the ignored source-mirror count separately, and preserve ambiguity when multiple external targets remain. Cover source mirror plus one external target and source mirror plus two external targets, then verify the real refresh route readback. |
| `agent_scoped_user_gate_overreach` | `quota should-run --agent-id <agent-a>` returns `user_gate`, `requires_user_action=true`, or `delivery_allowed=false` because an open user todo exists, but that todo is scoped to `<agent-b>` through `blocks_agent` or `claimed_by`; agent A has an independent runnable todo or only needs its own workspace/scope repair. | quota payload with the same `--agent-id`, `user_todo_summary.first_open_items`, `blocks_agent`, `claimed_by`, `agent_lane_next_action`, active state user/agent todos, related interaction pattern. | User-gate projection treated agent-scoped routing metadata as diagnostic text instead of a hard scope boundary, so a target-agent gate became a global owner gate. | Filter other-agent `blocks_agent` / `claimed_by` user todos out of the current agent's blocking summary while preserving diagnostic visibility; if the raw state was `operator_gate` only because of that other-agent gate and the current agent has runnable work, project an eligible current-agent lane. Cover with `examples/control_plane/quota-agent-scoped-user-gate-smoke.py` and workspace-guard smoke coverage. |
| `user_action_binding_projection_gap` | A non-blocking user action intended for one peer appears in another peer's heartbeat, or its scoped summary has `open_count=0` while inherited goal state still produces `operator_gate` / `NOTIFY`. | Active user-todo metadata, registered agents, agent-scoped quota summary and diagnostics, inherited status state, interaction contract user channel, todo mutation actor. | User actions had no first-class goal/agent binding, `claimed_by` was overloaded as routing, and quota filtered item details without clearing the upstream operator-gate state. | Require every multi-agent user todo to declare `bound_agent` or `goal_bound`; keep gate scope separate, make the bound agent the response-continuation lifecycle actor, filter other-lane details and counts, and override stale operator-gate state before interaction projection. Preserve `claimed_by` only as a read compatibility fallback and cover the no-current-work quiet lane. |
| `same_agent_scoped_gate_due_monitor_gap` | A same-agent user gate has an exact `unblocks_todo_id` or narrow `decision_scope`, an unrelated capability-runnable continuous monitor is due, but quota returns `must_attempt=false` and blocks the whole agent lane. | quota payload `scoped_user_gate_fallback`, `agent_todo_summary.monitor_due_items`, capability gate, gate relation, linked todo, and current-agent monitor metadata. | Scoped-gate fallback considered only advancement candidates, so the separately projected due-monitor lane disappeared before dependency filtering. | Add the already scoped, due, capability-runnable monitor projection to scoped-gate dependency filtering and preserve normal todo priority ordering; keep linked work blocked and reject monitors with missing capabilities. Cover both positive and negative cases in `examples/control_plane/quota-agent-scoped-user-gate-smoke.py`. |
| `mixed_monitor_capability_projection_split` | One due monitor is capability-runnable and another is capability-blocked, but quota selects the runnable monitor while `capability_gate.action=skip`, the work-lane due count includes both, and the interaction contract claims every candidate is unavailable. | quota `selected_todo`, `work_lane_contract.monitor_due_items`, `agent_todo_summary.monitor_due_count`, capability-gate runnable/blocked candidates, and the same `--available-capability` inputs. | Capability filtering split due monitors into runnable and blocked projections, then the capability gate consumed only the blocked projection while the work-lane count recomputed from unfiltered monitor items. | Feed both runnable and blocked due-monitor projections into the capability gate, keep blocked diagnostics, and make projected runnable due count authoritative for the work lane. Cover a mixed private-read/network pair in `examples/control_plane/monitor-scheduler-contract-smoke.py`. |
| `monitor_debt_advancement_starvation` | Overdue monitor catch-up produces repeated unchanged no-spend turns while same- or higher-priority advancement remains runnable; a scoped user-gate fallback may keep reselecting the monitor even after the ordinary work lane chose advancement. | current-agent run history excluding scheduler/accounting/state-refresh bookkeeping, monitor material-change flags, scoped fallback selection, work-lane reason codes, and Lark inbox urgency. | Monitor fairness was applied only to the ordinary work-lane contract; scoped fallback rebuilt and re-sorted candidates without the recent unchanged-monitor streak. | After two consecutive unchanged monitor-only work turns, prefer same- or higher-priority runnable advancement in every candidate-selection seam and expose compact arbitration evidence. Any non-monitor work or material monitor transition resets the streak; `lark_event_inbox.reply_due` still preempts all ordinary lanes. Cover scoped fallback starvation, terminal transition reset, and reply-due precedence. |
| `unscoped_multi_agent_user_gate` | A peer sees a `user_gate` todo that belongs to another lane because the todo has no `blocks_agent` and no explicit `global_gate=true`; every `quota should-run --agent-id ...` call may project it as a current owner gate. | active-state user todo metadata, registered agents, quota `user_todo_summary`, current `--agent-id`, CLI authoring command, interaction contract user channel. | The authoring layer allowed open multi-agent user gates without machine scope, relying on prose and agent memory to infer which lane was blocked. | Reject new multi-agent `user_gate` writes unless they specify `blocks_agent` / `--agent-id` or explicit `global_gate=true` / `--global-gate`; make `loopx check` flag existing unscoped gates so old/manual state must be repaired. Cover with `examples/control_plane/todo-user-gate-scope-smoke.py`. |
| `consumed_user_gate_writeback_gap` | The user says a projected review or action already happened, but quota keeps returning `user_gate` / `operator_gate` and no successor or replan appears. | exact user todo id and lifecycle metadata, latest user statement or typed external evidence, quota before and after lifecycle writeback, current agent vision and runnable frontier. | The completion fact stayed in chat or an external surface instead of transitioning the durable todo, so gate precedence correctly hid the empty-frontier replan. | Complete only when an explicit decision outcome grants or rejects the exact scope; otherwise supersede the stale gate without consuming authority. Refresh state, rerun scoped quota, and require a successor or `autonomous_replan_required` when an active vision still has no runnable advancement. |
| `agent_scoped_no_candidate_gap` | `quota should-run --agent-id <peer>` returns `should_run=true`, `delivery_allowed=true`, and `quiet_noop_allowed=false`, but `agent_lane_next_action` is empty, `current_agent_claimed_advancement_items` is empty, and no runnable candidate is projected for that peer; the recommendation may still point at another peer's lane. | quota payload with the same `--agent-id`, active state claimed todos, registered peer profile/scope, recent history, open review blockers. | Agent-scoped quota does not distinguish "goal has runnable work" from "this peer has runnable work"; other-agent or out-of-scope candidates keep the heartbeat eligible. | Project `reassignment_required` when only other-peer claims remain, or `agent_scope_wait` for an explicit blocking review dependency. Allow quiet no-op/no-spend until a concrete current-agent or unclaimed todo is available. |
| `agent_scoped_replan_broadcast_gap` | `quota should-run --agent-id <peer>` returns `autonomous_replan_required` from an unscoped replan obligation even though that peer has no runnable advancement work and should wait quietly or run only due monitor work. | quota payload with the same `--agent-id`, `autonomous_replan_scope`, `goal_route_hint`, obligation owner fields/triggers, active agent/monitor todo summaries. | Goal-level replan was treated as a broadcast obligation before peer task ownership was resolved. | Apply explicit replans only to their owner; resolve unscoped replans through the current goal frontier or deterministic task coordination, and let unrelated peers fall back to `reassignment_required`, `agent_scope_wait`, or `monitor_quiet_skip`. Cover with the replan decision-plane smoke. |
| `agent_scoped_spend_attribution_gap` | A peer writes a validated accountable delivery and then runs scoped `quota spend-slot`, but `delivery_run_generated_at`, classification, and agent id are null after another peer writes a newer refresh or spend event. | scoped spend payload, requested `agent_id`, recent goal run index ordered by time, same-agent accountable refresh, intervening other-agent refresh/spend events. | Latest-unspent lookup stopped at the first goal-wide event instead of scanning the requested agent's event stream, so concurrent peers broke causal attribution. | For explicit `agent_id`, skip events attributed to other agents and stop only on same-agent or legacy unscoped terminal/accounting events; keep unscoped lookup behavior unchanged. Cover target delivery -> other-agent delivery/spend -> target scoped spend with identity assertions. |
| `accountable_refresh_after_spend_gap` | A generated host prompt says to spend before a progress refresh; the spend receipt points to the previous delivery, while the new `outcome_progress` refresh remains unspent and may be consumed by a later turn. | generated full/compact/brief/thin host prompts, `progress_refresh_state_command`, `quota_spend_command`, `refresh_state_command`, spend receipt delivery timestamp/classification, recent run index. | Accountable delivery refresh and optional state-only dashboard refresh were collapsed into one post-spend step. | After validation and other writeback, emit one accountable progress refresh, then spend exactly once against it. Only an optional plain state-only refresh may follow spend; never emit another accountable progress refresh after accounting. Cover actual generated command order for every prompt family. |
| `watch_replan_safe_bypass_spend_gap` | A due monitor finds no external or code change, but the agent rewrites a user gate, monitor target, or Next Action, labels the refresh `outcome_progress`, and spends through a safe bypass. | selected todo task class, monitor result/material-change flag, refresh `autonomous_replan_ack.delta_contract`, delivery outcome, latest unspent delivery lookup, spend receipt. | Any changed Next Action counted as a replan frontier delta, caller-supplied outcome remained authoritative, and safe-bypass accounting did not require a causal accountable writeback. | Keep watch/readback deltas valid for state acknowledgement but normalize them to `outcome_gap` unless the executable, blocked, or terminal frontier changes; require a latest unspent accountable delivery before every safe-bypass spend. Cover weak-versus-material replan deltas and the safe-bypass modes with and without a delivery run. |
| `agent_scoped_cli_identity_projection_gap` | `quota should-run --agent-id <peer>` identifies the registered peer, but generated `refresh-state`, `monitor-poll`, or `spend-slot` actions omit `--agent-id`; copied commands then append goal-scoped runs or accounting rows with `agent_id=null`. | quota `agent_identity`, interaction-contract `next_cli_actions`, recent run index agent ids, and scoped versus unscoped command fixtures. | Interaction-command rendering treated peer identity as diagnostic context instead of a causal writeback/accounting argument on every generated state command. | When the source quota call has an agent id, preserve it on every generated refresh, monitor, spend, and follow-up quota command; keep deliberately unscoped/controller calls unchanged and cover both scoped and unscoped command packets. |
| `selected_registry_roster_precondition_masked` | `quota should-run --registry <project> --agent-id <peer>` fails as `quota_unexpected_collection_error` when the selected goal lacks `coordination.registered_agents`, even if another registry knows the peer. | exact selected registry, requested public-safe agent id, selected goal coordination block, any global registry with the same goal id, default and verbose failure payloads. | Identity admission raised an untyped `ValueError`, so the public-safe CLI boundary correctly hid it with the generic collection envelope but also erased the actionable registry precondition and requested identity. | Keep the explicitly selected registry authoritative and never fall back silently. Raise a typed public-safe identity precondition carrying a bounded machine code, the normalized agent id, and selected-registry recovery; retain the generic envelope for arbitrary `ValueError`, and cover the real CLI route with a conflicting global roster. |
| `deferred_gate_resume_misclassified` | A ready deferred successor with `resume_when` or `unblocks_todo_id` is visible, but quota/status treats the peer turn as no-candidate, `agent_scope_wait`, or quiet no-op. | quota payload deferred lanes, active state deferred todo metadata, `resume_condition`, `resume_ready`, claimed_by split, related user/review gate todo. | Deferred work was modeled as absence of todo instead of a gate-resume lifecycle condition. | Route ready current-agent/unclaimed deferred work through `successor_replan_required` / `deferred_resume_projection`; keep deferred visibility after open lanes; only fall through to no-candidate wait when no ready deferred resume candidate exists. |
| `resume_gated_open_todo_selected` | An open todo with `resume_when` and `resume_ready=false` appears in `first_executable_items`, `executable_backlog_items`, `capability_gate.runnable_candidates`, `agent_lane_next_action`, or `recommended_action`; a lower-priority safe fallback exists but is hidden behind the not-yet-unlocked successor. | quota payload with the same `--agent-id`, active-state todo metadata for the selected todo and its `resume_when` target, `resume_condition.target_status`, status `agent_todo_summary`, catalog canary plan/run output. | The shared executable-candidate path treated visible open todos as runnable after one projection seam already filtered them elsewhere. | Make open todos with `resume_when` non-executable until `resume_ready=true` across all executable projections, add a focused quota smoke for the positive and negative cases, and wire the touched policy seam into catalog canary domain checks before generic family checks. |
| `pr_merge_event_projection_gap` | Issue-fix lifecycle state records a terminal merged PR and its monitor closes, but a successor with `resume_when=pr_merged:#N` remains `resume_ready=false`. | issue-fix PR lifecycle row, completed monitor evidence, rollout event log, successor `resume_condition`. | The lifecycle writeback persisted domain state without appending the public `pr_merge` rollout event consumed by todo resume projection. | When a connected lifecycle writeback observes `MERGED`, append one repo-qualified, public-safe, idempotent `pr_merge` event; replay must reuse the same event and immediately satisfy the dependent resume condition. |
| `pr_merge_resume_repository_collision` | A todo with `resume_when=pr_merged:#N` becomes ready because an unrelated repository merged the same PR number, or a cross-repository dependency cannot be distinguished from its execution repository. | todo `task_repository`, qualified/unqualified resume ref, compact public `pr_merge` rollout events, projected `resume_condition`. | Resume matching treated a bare PR number as globally unique and ignored the todo's repository identity. | Bind unqualified refs only to the todo's GitHub `task_repository`; require `owner/repo#N` for cross-repository dependencies; fail closed with a machine-readable ambiguity when no repository can be derived. Cover identical PR numbers across repositories without reading bodies or comments. |
| `handoff_gate_state_projection_gap` | A `blocks_agent` handoff/review todo changes status, but the blocked agent's quota gate does not change immediately: open review work may be hidden behind stale done handoffs, or a done review without a successor falls through to generic `agent_scope_wait`. | quota payload `agent_scope_frontier`, status `agent_todos.handoff_gates`, active-state todo metadata `status`, `done`, `blocks_agent`, `unblocks_todo_id`, `superseded_by`, and successor links. | Handoff lifecycle was inferred from open todo lanes instead of a small state machine, so done/superseded handoff todos vanished from gate inputs or stale done gates outranked live blockers. | Project `todo_handoff_gate_v0` from the complete todo list: `blocking`, `cleared_without_successor`, `cleared_with_successor`, `superseded`, and `deferred`. Quota should prefer current-agent `blocking` gates, map `cleared_without_successor` to `successor_replan_required`, run concrete successors normally, and ignore superseded stale gates. Cover with `examples/control_plane/quota-cleared-blocker-successor-gate-smoke.py`. |
| `inter_agent_handoff_priority_gap` | A peer has same-priority runnable work, including explicit review todos that unblock another peer via `blocks_agent` and `unblocks_todo_id`, but the handoff is buried behind ordinary backlog. | quota `agent_lane_next_action`, `capability_gate.runnable_candidates`, todo continuation metadata, registered peer identities, active state Next Action. | Explicit review dependencies were visible but absent from the runnable-candidate tie-breaker. | Rank current-peer claimed unblock/review work ahead of ordinary same-priority backlog in both selected-action surfaces; add a smoke that the selected lane and first runnable candidate agree. |
| `task_coordinator_worker_drift` | A peer selected as temporary coordinator repeatedly executes one child lane itself while other eligible child lanes remain open under `multi_subagent`. | quota `task_orchestration_contract`, goal orchestration policy, claimed-by-others lanes, work-lane contract, `interaction_contract.agent_channel.primary_action`, generated heartbeat prompt. | Child lanes were diagnostic-only and the task-scoped coordinator contract still said to execute the first todo directly. | Project eligible child lanes into `task_orchestration_contract`; tell the selected coordinator to spawn/resume the bounded bundle and aggregate accepted evidence. Do not persist coordinator status as agent rank. |
| `subagent_receipt_runtime_identity_gap` | A real host reaches child collaboration and returns a completed child receipt, but reconciliation rejects it because a runtime, worker, or evidence reference contains prose, a URL, an executable, a workspace, a Session file, or another local path. | Host-specific structured-output schema, compact collaboration event types, raw-field safety classification without path values, normalized child receipt, and reconciliation counts. | Runtime validation required opaque references, but the structured-output schema constrained only lengths and the host contract did not pin its stable runtime identity, so the model substituted natural language or local execution context. | Align the structured-output schema with opaque-reference validation, require at least one opaque evidence token, pin `runtime_id` to the stable host id, explain that `worker_ref` is opaque, retain generic public-safety rejection, and run a real isolated child probe plus focused schema/prompt tests. Keep live-tool interception and automatic child termination outside any observation-only claim. |
| `stale_recommended_action` | The agent repeats "launch or preflight" after the active state or evidence already narrowed the next action. | latest active state, quota payload timestamp/content, recent history rows. | Recommended action was not regenerated from the newest state. | Refresh state, update recommender/projection tests, and write an exact next action. |
| `stale_active_next_priority_override` | A lower-priority todo named by durable `Next Action` becomes `agent_lane_next_action` even though the same peer has a higher-priority capability-runnable todo, commonly after a deferred P0 resumes or another peer refreshes shared state. | scoped quota payload, `capability_gate.runnable_candidates`, `agent_lane_next_action`, active-state `Next Action`, todo priority/resume/claim metadata, recent peer writes. | The active-next hint was ranked before todo priority, so stale steering text acted as a global override instead of a same-priority tie-breaker. | Preserve peer claim/profile routing, then rank todo priority before active-next preference; keep active-next as a same-priority tie-breaker. Cover a resume-ready P0 plus lower-priority active-next and concurrent other-peer work. |
| `status_projection_history_neutral_gap` | `quota should-run` reports no user action or quiet monitor behavior, but `status` / `diagnose --limit N` misreports a stale controller/user gate or older connected-without-run state after recent monitor/spend/readiness events. | quota payload, diagnose/status payload, recent history classifications, neutral-run classification sets, UI `--limit`, latest meaningful state run. | Status/history/quota disagree on which runs are status-neutral, or a short display limit is reused as the control-plane reasoning window. | Share the neutral classification contract across history/status/quota and reason over a wider internal state window before trimming displayed runs; add a regression with consecutive neutral runs before a meaningful state transition. |
| `monitor_replan_noop_loop` | Recent typed observations or monitor targets are materially equivalent, yet maintenance writebacks keep succeeding and the same action returns without a new surface, hypothesis, probe family, runnable successor, blocker, or coverage-backed terminal. | quota packet, shared goal-frontier context, typed progress fingerprints, current obligation id, host coverage ledger, current state Todo ids, and semantic receipt. | Activity/prose was treated as progress, or the write gate reduced only run-history while quota derived a frontier/vision obligation. | Trigger after two equivalent typed fingerprints; recompute quota's full goal frontier at write time; reject maintenance before durable write; close only on an accepted semantic delta bound to the exact obligation. Never infer semantic state from classification or recommendation strings. |
| `watch_lane_ack_causality_gap` | An old periodic or monitor ACK is followed by a newly derived vision/frontier obligation, but the stale ACK or a new maintenance-only writeback suppresses it. | old and current obligation ids, generation order, three vision/checkpoint acceptance gaps, current monitor Todo lifecycle, typed observation, and persisted semantic receipt. | ACK identity was not bound to the obligation it closed, and write-time enforcement saw only run-history-derived duties. | Bind every semantic receipt to the exact current obligation id; share the full goal-frontier reducer between quota and refresh-state; allow bounded watch Todo lifecycle to suppress an inapplicable future wait before replan derivation, but never treat watch continuation as semantic replan closure. Cover old ACK→new vision/frontier rotation and reject the first maintenance write before state/run persistence. |
| `vision_replan_writeback_gap` | The user or agent identifies a missing outcome, acceptance condition, or product bottleneck, but later `quota should-run` shows no `goal_frontier_projection.acceptance_gaps[]`, no per-agent `agent_vision` / `vision_checkpoint_v0`, and no replan obligation; the agent keeps following older runnable todos or monitor waits. | recent user correction, `loopx history --goal-id`, latest runs' `agent_vision` and `vision_checkpoint`, quota `goal_frontier_projection.acceptance_gaps`, active-state todos, bad-case report or incident note. | The insight stayed in chat, prose, or a report instead of being converted into a bounded `goal_vision_replan_contract_v0` packet, per-agent checkpoint decision, or concrete successor todo. | Write `loopx refresh-state --agent-id <agent-id> --vision-summary ... --vision-replan-trigger ...` or `--agent-vision-json <packet>` with a compact `replan_trigger_summary`; use `--vision-unchanged-reason` when the current per-agent vision still applies; and add/link a successor todo when the next executable step is known. Then rerun quota so the vision gap or `vision_checkpoint_missing` can become `autonomous_replan_required` before local quiet/wait states. |
| `vision_checkpoint_rearm_loop` | An accepted vision replan alternates with ordinary work on the same Todo, and a later `unchanged_with_reason` closeout immediately creates another `vision_outcome_checkpoint_required`; an agent may also reconstruct `semantic_closeout` even though quota projected `in_flight_continuation`. | exact quota settlement template, selected Todo id, delivery boundary/outcome, latest accepted semantic ACK, latest agent vision revision and evidence-linked path, latest outcome checkpoint decision and continuity basis. | The TypeScript checkpoint accepted an unchanged decision without exporting which vision revision it covered, so the outcome reducer rejected every unchanged checkpoint; weak agents also summarized quota and rebuilt writeback instead of preserving the projected flags. | Bind `unchanged_with_reason` to the exact persisted vision revision in the TypeScript checkpoint and accept it only while that same revision still carries an evidence-linked legal path outcome. Preserve accepted ACKs behind newer rejected attempts. Require heartbeat agents to start from `cli_channel.next_cli_actions[0]` and keep every projected boundary/identity flag. A missing checkpoint, changed revision, or evidence-free path must still re-arm. |
| `active_goal_closed_vision_succession_gap` | A bounded stage is correctly marked `vision_closed`, but the registry goal remains active; quota shows no acceptance gap or exposes one without requiring replan, then continues ordinary or resume-ready deferred work or monitor quiet without establishing the next vision. | latest per-agent `agent_vision`, registry goal status, quota `goal_frontier_projection`, remaining open/deferred advancement todos, terminal lane rationale. | Closed vision was treated as terminal, or a generic ready-deferred shortcut returned before the successor-vision gap was evaluated, so closing one accepted stage erased or demoted the long-horizon vision frontier. | Treat `vision_closed` as stage closure: for an active registry goal, project `vision_successor_required` and run replan even when ordinary or resume-ready deferred advancement already exists. Explicit user/blocking handoff gates still take precedence. Only `retired`, `superseded`, or `no_followup` may terminate the lane without another vision. Cover active/inactive goal and runnable/deferred/empty frontier cases. |
| `scheduler_liveness_backoff_gap` | `quota should-run` says `should_run=false`, `quiet_noop_allowed=true`, no spend, or `automation_action=keep_active_quiet`, but the host keeps polling at the bootstrap cadence for hours; Codex CLI TUI or Claude Code `/loop` also keeps repeating unchanged checks. | quota payload `automation_liveness`, `interaction_contract`, `heartbeat_recommendation`, host automation cadence, CLI/Claude loop logs or statusline, recent no-spend poll history. | LoopX separated execution permission from user notification, but did not expose a machine-readable next-wakeup/backoff/final-check/exit policy for host runtimes. | Add or repair `quota should-run.scheduler_hint`; host runtimes must apply Codex App cadence backoff and CLI/Claude unchanged-poll final quota/replan check before self-stop, without quota spend. Prompt/docs should treat fixed 3-minute cadence as bootstrap only. |
| `monitor_host_floor_deadline_violation` | A monitor declares a cadence or next due time below 15 minutes, but Codex App remains on the `[15, 30, 60]` quiet-wait progression and wakes after the monitor is due. | quota scheduler detail `cadence_context`, monitor `cadence`/`next_due_at`, recommended and observed host RRULE, host-update failure state, recent wake timestamps. | The quiet-monitor 15-minute backoff floor was applied before the explicit monitor cadence/due cap, turning an efficiency default into a deadline floor. | Derive the effective host floor from the tightest explicit monitor cadence or due horizon; retain `[15, 30, 60]` only when no tighter wake is required. Cover both sub-15 cadence and sub-15 due-horizon cases with the monitor scheduler smoke. |
| `terminal_no_followup_retrigger` | Complete valid user/agent todo sources carry no-follow-up closure evidence and the normalized goal frontier is empty, but `quota should-run` still returns `eligible`, `must_attempt_work=true`, or `scheduler_hint.action=run_now`; another form emits a stop hint but the Codex App heartbeat remains active. | derived `goal_frontier_projection.terminal_state`, source completeness, todo summaries, execution obligation, automation liveness, scheduler hint, generated heartbeat prompt, and active host automation state. | Quota treated every `waiting_on=codex` goal with compute as runnable, trusted a free-form status marker, or exposed a host action that the heartbeat prompt never executed. | Derive read-only terminal state only from valid complete todo sources, closure intent, and an independently empty frontier; fail closed on missing/malformed data or any open dimension. Project no execution/spend plus `stop_until_explicit_resume`, and require the heartbeat to call `automation_update` once and verify the stop. |
| `scheduler_reset_application_gap` | Codex App heartbeat cadence backs off, but repeated unchanged polls do not advance the progression; after user feedback, a new/reassigned todo, resolved gate, material transition, or `run_now`, the automation remains slow; LoopX reports `apply_needed=false` while the installed App RRULE differs; or a matching reset RRULE still demands a no-op host update before ACK. | actual automation RRULE, quota payload `scheduler_hint.codex_app.stateful_backoff` and `host_observation`, `codex_app.recommended_rrule`, `codex_app.ack_hint.cli_args`, `quota scheduler-ack-current` result, recent user interaction/todo/history transition. | The scheduler ACK ledger was treated as host truth even though ACK can precede/fail to follow `automation_update`, or the host can later drift; matching host evidence was also lost when `scheduler-ack-current` recomputed the decision. | Keep the CLI-owned ACK protocol, but reconcile a uniquely matched active heartbeat's observed RRULE before suppressing work. A mismatch must set `host_observation.status=drift_detected` and re-emit `recommended_rrule`; a matching unbound reset sets `ack_needed=true` and carries its exact reset token, identity, and CLI route into a no-write ACK. Missing readback remains `apply_needed=true`. Never read the prompt body into the hot payload or edit App manifests directly. Cover false/stale ACK, ambiguous host-update result, real cadence change, host match, and reset-to-`run_now` with smokes. |
| `skill_cli_contract_drift` | A skill, installed release snapshot, generated prompt, smoke fixture, or agent handbook suggests a CLI form that the current parser rejects, or tells an already activated Goal to bootstrap another Goal. | `loopx <subcommand> --help`, current host task contract, failing command stderr, `rg` over `skills/`, installed skill copies, release snapshot, generated prompts, and relevant smoke fixtures. | Process documentation drifted from the CLI or host lifecycle contract, or a fixture preserved old command text without labeling it as legacy/non-executable. | Update the durable skill/source docs and installed skill copy, make activated continuation follow the exact CLI `interaction_contract` before bootstrap, add or update a focused smoke, and keep legacy fixture strings only when the test explicitly asserts they are untrusted historical payload. |
| `effect_runtime_source_topology_fingerprint_gap` | A control-plane `.ts` or contract `.json` file is added or removed, but the managed Effect runtime fingerprint does not rotate and an older process remains eligible for reuse; or a concurrent install/update leaks `FileNotFoundError` between inventory, metadata collection, and content hashing. | Exact source-file inventory before and after the change, directory metadata on the affected filesystem, fingerprint values, bounded full-fingerprint attempts, and managed runtime process identity. | Source topology caching treated directory `mtime`/`ctime` as authoritative change evidence even though some filesystems preserve that metadata across rapid entry changes; deleting that cache also deleted its bounded race recovery, and restoring the retry around metadata alone left the later content-read boundary exposed. | Discover the bounded owned source inventory on each fingerprint check, retain content hashing behind the file-metadata snapshot cache, retry the complete inventory-plus-metadata-plus-content-hash operation once when a source disappears, then raise a stable diagnostic if churn continues. Cover unchanged directory metadata, added and removed files, deterministic scan/stat disappearance, and stat/read disappearance through request and readiness. Do not add a watcher or TTL. |
| `local_release_snapshot_stale_after_merge` | Source checkout or `PYTHONPATH=<repo> python -m loopx.cli` shows a fix, but the default `loopx` command still returns the pre-fix quota/status behavior immediately after a self-merge or fast-forward. | `which loopx`, `readlink ~/.local/bin/loopx`, `loopx doctor` install freshness and promotion mode, default `loopx ...` payload, source-run `PYTHONPATH=<checkout> python3 -m loopx.cli ...` payload, latest `origin/main`. | The default command is a release snapshot wrapper that forces `PYTHONPATH` to its release root; the source checkout was updated but the installed local release was not refreshed. | Refresh with `loopx update --execute --ref main` or a clean `origin/main` checkout's `scripts/install-local.sh`, rerun the default command without `PYTHONPATH`, and write back the repair. Dirty/non-main worktrees are canary-only unless an explicit validated promotion uses `LOOPX_PROMOTE_DEFAULT=1`. |
| `stale_local_service_reused_after_upgrade` | The default command and desktop assets contain a fix, but dashboard mutations still return a pre-fix error after an upgrade or reinstall; a UI-only development preview may instead expose an empty `502` response as a raw JSON parser failure. | `readlink ~/.local/bin/loopx`, release manifests for the selected command and listeners, listener process start times, `LOOPX_RELEASE_ROOT` for the status/Chat services, Vite proxy target reachability, and loopback capability payloads. | A long-lived local status or Chat process survived the upgrade, the UI was started without its matching Chat service, or the launcher accepted a schema-only service fingerprint and silently reused code from another release. | Expose a public-safe runtime identity (`package_version`, immutable `release_id`, and source revision) on local service probes. Start development UI and Chat as one version-matched surface; parse API failures at the shared client boundary so empty or non-JSON responses become actionable service errors. Reuse only an exact installed-release match; classify a valid LoopX service from another release as stale and require its owning dashboard/app to stop before the current release starts. Cover browser launcher, UI-only proxy failure, and desktop supervisor probes. |
| `release_evidence_goal_binding_gap` | A release/promotion canary passes every check, then its evidence writeback fails because a fixed or caller-selected project Goal is absent from the registry. | canary command result, evidence write command, registry goals, runtime root, promotion gate and doctor readback. | Installation-scoped release evidence reused project `refresh-state`, so an unrelated Goal became a hidden prerequisite for validating the shared local release. | Store new evidence in an append-only runtime release ledger through an explicit CLI write contract; read it for every Goal and retain old Goal run-history events only as a compatibility fallback. Cover an empty-goal registry plus installer, gate, doctor, and goal-filtered status readback. |
| `local_release_python_runtime_drift` | A locally promoted release imports under CI's declared Python but the default macOS `python3` exits during CLI startup on a newly added standard-library symbol. | `python3 --version`, default wrapper target, failing import traceback, `pyproject.toml` requirement, changed module imports, candidate doctor under the actual wrapper interpreter. | A new module used a newer convenience alias even though the source-based local wrapper still runs on an older system interpreter, and release validation did not exercise that real default path before promotion. | Prefer an equivalent broadly available standard-library API at the narrow import seam, add a focused timestamp/import regression, run candidate doctor plus the affected product smoke with the actual default interpreter, and only then promote the repaired release. Do not claim broad interpreter support beyond the verified CLI path. |
| `heartbeat_prompt_error_projection_gap` | `loopx heartbeat-prompt --help` succeeds, but an invalid generation command exits 1 while markdown shows a copyable empty task body and `Generator Inputs` fields such as `active_state`, `thin`, `agent_id`, or `agent_scopes` as `None`. | failing markdown output, `--format json` payload, CLI handler exception branch, active-state lookup inputs, agent identity arguments. | The command caught generation errors into a minimal `{ok:false,error}` payload and then reused the success markdown renderer, which made an error look like a generated blank automation. | Build a heartbeat-prompt error payload that preserves requested inputs, render a distinct error page with no copyable task-body fence, and cover markdown plus JSON failure output with a focused smoke. |
| `shell_pr_comment_command_substitution` | A PR comment, issue body, or review body is posted with shell command substitution artifacts because Markdown backticks were passed through a double-quoted `gh ... --body "..."` argument. | shell command used for the PR/comment operation, rendered public comment body, recent agent transcript, and PR review/comment hygiene instructions. | Agent treated Markdown as inert text inside a shell string; the shell evaluated backticks before `gh` received the body. | Do not pass Markdown-rich bodies through double-quoted shell arguments. Use a single-quoted body when safe, stdin or `--body-file` for multiline text, or a GitHub connector/API call that bypasses shell interpolation; add a smoke or checklist item when the workflow recurs. |
| `external_delivery_clock_override` | A caller can move an external send into or out of a configured delivery window by supplying an artifact timestamp such as `--generated-at`. | execute-path CLI arguments, sink window decision input, current invocation time, focused zero-provider-call smoke. | One caller-controlled timestamp was reused for both reproducible artifact metadata and runtime delivery authorization. | Keep artifact time and execution-observation time separate. Bind live CLI delivery decisions to a trusted invocation clock, expose any injected clock only as an internal test seam, and prove that an in-window artifact timestamp cannot override an out-of-window execution observation. |
| `reviewer_notification_semantic_dedupe_gap` | A reviewer was already tagged by a clear public review-request comment, but a later automated fallback posts another comment because the earlier message lacks the current idempotency marker or uses different handle casing. | live PR comments, normalized selected reviewer, request/comment write calls, marker receipts, reviewer-request packet. | Idempotency recognized only system-authored markers and ignored older or human-authored explicit review-request evidence. | Keep marker matching as the fast path, then recognize only bounded, direct review-request language near a normalized reviewer mention; ignore quotes, code, ordinary discussion, and weak mentions. Return the existing public comment URL and perform zero writes. |
| `nested_sink_checkpoint_overwrite` | An inner resumable sink successfully persists newly created remote record ids, but an outer digest/checkpoint write restores the pre-sync config snapshot; remote rows exist while the local record map remains stale. | sink receipt written/skipped counts, config record-map count before/after, outer checkpoint code, immediate idempotent retry. | Nested writers loaded the same config before delivery, then the outer writer saved its stale snapshot after the inner writer committed incremental state. | After successful inner delivery, re-read the persisted config and merge only the outer checkpoint fields into that latest payload. Add a smoke where the inner sink mutates the record map and assert the outer digest write preserves it. |
| `todo_text_update_continuation_residue` | `todo update --text` reports success, but status and later monitor writeback still append an older indented description after the new text. | active-state todo block, metadata-line position, `todo list` parsed text, update payload before and after monitor writeback. | Text replacement removed only continuation lines before the first metadata marker, while structured metadata had been inserted ahead of legacy continuation lines. | Replace the todo marker line, remove every indented non-metadata continuation within the same todo block, preserve structured metadata, and cover a fixture where stale prose follows the metadata marker. |
| `external_sink_authority_gap` | A local state refresh must proceed while the current runtime boundary forbids external writes, but an enabled projection automatically invokes its configured remote sink; agents toggle the whole feature off or risk an unauthorized write. | goal feature policy, refresh command, sink activation packet, current external-write authority/capabilities, sink digest before/after. | Feature opt-in and per-run delivery authority were represented by one boolean, so local canonical projection could not be separated from remote delivery. | Add a run-scoped external-sink suppression boundary that preserves canonical local projection, performs zero remote writes, does not advance sink digests, and remains retryable after authorization returns. Keep goal-level feature opt-in unchanged and cover the real refresh call path. |
| `sink_binding_activation_gap` | A generated heartbeat asks for Kanban or graph sync while the corresponding goal feature is disabled, or an old local board binding causes sink repair work to displace a runnable project P0. | generated heartbeat prompt, goal Explore Graph and generic Kanban switches, quota/status goal boundary, local binding presence, selected todo before and after sink failure. | The heartbeat template treated sink discovery or configuration presence as activation and emitted one unconditional delivery rule for unrelated sink types. | Remove sink-specific activation from the host prompt. Let Explore Graph activation remain inside the goal-aware material refresh path; keep generic Kanban user-command-only unless a separate goal opt-in projects one concise executable post-writeback action through quota/status. Treat local bindings as inert, keep generic Kanban failures nonblocking, and never let sink follow-up preempt runnable P0. Cover action absence by default, explicit opt-in projection, and the real quota interaction contract. |
| `explore_graph_delivery_postcondition_gap` | Graph-on is enabled and local projection changes, but the agent delivers before the configured board is synced, or a sink failure is only a warning and the remote board silently stays stale. | goal `explore_graph` policy, material refresh packet, sink semantic/readback digests, row/result ids, current external-write authority, final delivery timing. | Feature activation scheduled best-effort projection but did not expose or enforce a material-delivery postcondition; the agent also treated graph sync as a deferrable P1. | Require configured sink sync plus row/result-id readback after material refresh. Authorized failure blocks delivery and remains retryable; suppressed external delivery preserves local state but requires a concrete successor. Keep Explore Graph and Explore Harness opt-ins independent. |
| `explore_sink_source_runtime_bypass` | A goal-scoped Lark/Explore sink proposes an older or smaller graph after another runtime already delivered a fresher projection; retrying would overwrite or visually regress registered rows. | invoked registry role, goal `source_registry`, compact source-runtime route, candidate result-id coverage, registered sink result ids, semantic/readback digests, and connector call count. | The sink projected directly from the invoked registry runtime instead of the registry-declared canonical source, and digest comparison could not distinguish a legitimate update from source shrinkage. | Resolve the canonical goal source before projection, expose only hashed route diagnostics, and enforce the append-only registered result-id coverage guard before previews or remote writes. Fail closed with zero connector calls and a source-reconciliation successor when registered ids disappear. |
| `explore_owner_view_semantic_collapse` | Explore Base rows, digests, and whiteboard readback all succeed, but the owner-facing visual becomes a chronological card wall, fake epoch sequence, or decorative summary that hides the real work lanes, frontier, and material relations. | canonical Nodes/Edges/Findings, rendered SVG/Mermaid source, renderer metadata, exported whiteboard image, owner reference surface. | Delivery correctness and geometric stability were accepted as semantic/visual correctness; the renderer owned placement while discarding the graph's actual edge and lane meaning. | Use a configurable semantic-board renderer that derives lanes from canonical lineage/tags, highlights frontier from current status, and draws actual visible relations. Require semantic counters plus exported-image inspection before delivery; sink/readback success alone is insufficient. |
| `explore_stage_document_membership_gap` | Stage whiteboard updates and marker readbacks succeed, but the target document omits configured stages or one view role removes another role's sections. | target document outline, configured stage tokens and section ids, role-specific stage counts, post-create outline readback, whiteboard marker receipts. | A readable board token was treated as proof of document membership, or multiple view roles shared one heading namespace and reconciled each other's sections as stale. | Give each role a disjoint managed-section namespace, recreate configured stages that are absent from the document outline, and fail closed unless a final outline readback contains exactly one managed section for every desired stage. |
| `explore_visual_cross_role_postcondition_gap` | Each role reports a successful marker readback, but a later role overwrites the same whiteboard and makes the earlier success stale. | configured role/stage token fingerprints, publish order, batch readback target set, final role markers. | Stage tokens were validated only within each role and marker settlement ran before all role writes completed. | Reject cross-role stage-token reuse before writes, then settle every role/stage marker once after the final batch write and recompute all role and delivery statuses from that final postcondition. |
| `nested_singleflight_self_contention` | A batch owns a sink singleflight lock, then a nested direct command or automatic sync for the same config returns `sync_busy` even though no independent writer exists. | outer batch lock scope, nested command call path, process/context identity, connector call count, cross-process contention smoke. | The file lock distinguished only acquired versus busy and could not recognize re-entry by its current execution context; a regression fixture may also have encoded self-rejection as expected behavior. | Track held lock targets in execution-local context, reuse only an exact same-target ownership, preserve cross-context/process fail-fast, and cover nested direct/decorated calls plus a competing process. |
| `pr_review_lifecycle_group_truncation` | `loopx pr-review --state all` reports open PRs but `review_groups.merged` is empty even though `--state merged` or GitHub closed/merged search finds merged PRs in the same window. | `loopx --format json pr-review --state all --since ...`, `loopx --format json pr-review --state merged --since ...`, `gh pr list --state closed/merged`, and `examples/pr-review-command-smoke.py`. | The live fetch or packet builder applied one combined `--limit` before lifecycle grouping, so open PRs consumed the packet and merged post-merge review entries were truncated away. | Fetch open and closed/merged windows separately for `--state all`, apply `--limit` per lifecycle group, keep `review_groups.unmerged` and `review_groups.merged` authoritative, and cover a fixture where `--limit 1` still returns one merged PR. |
| `pr_review_response_duplicate_replan` | A PR still reports `CHANGES_REQUESTED` after the author pushed a newer repair and resolved every review thread, so lifecycle polling repeatedly proposes another patch successor while the actual next step is reviewer re-approval. | Compact PR lifecycle metadata, complete review-thread counts, latest changes-requested timestamp, head commit timestamp, CI rollup, and current monitor todo. | Lifecycle routing treated the aggregate review decision as a current unhandled action and ignored the response state encoded by newer commits plus resolved threads. | On explicit metadata fetch, read only compact review-response evidence and route to a quiet re-review monitor when at least one thread exists, every fetched thread is resolved, pagination is complete, and the head commit is newer than the review. Missing, partial, empty-thread, or older evidence must fail closed to the actionable replan route. |
| `pr_review_explicit_selection_idempotency_gap` | A named PR receives another full evidence pass even though the packet marks its exact-head conclusion valid or its merged row has `review_action_kind=null`; generic `re-review` wording repeatedly reopens completed work, a null-action row remains in the ranked `review_sequence`, or its inventory row still carries an executable-looking review plan/template/commands that a host follows. | Selected packet row, exact head, `review_conclusion`, `review_action_kind`, plan/template/command presence, top-level and group `review_sequence`, summary attention counts, current-request wording, and the evidence-command tool trace. | The host adapter conflated explicit selection authority over queue ordering with authority to override the capability's exact-head execution decision, while the packet mixed bounded inventory with executable ranking and artifacts and treated generic re-review wording as a force-refresh token. | Project a typed selection-execution contract: explicit selection changes ordering only; null action is compact readback-only with null plan/template and empty evidence commands; every `review_sequence` and attention count derives exclusively from non-null actions; and a fresh audit on an unchanged/no-action row must be regenerated through the typed `--fresh-audit-exact-head NUMBER@HEAD_OID` option after an explicit request or concrete new concern/evidence invalidation. Keep valid concluded rows only in inventory; assign an explicit audit action to merged heads that genuinely lack a valid conclusion. Cover the packet contract, installed skill text, exact valid-merged fixture, and result-check rejection of inventory-only rows with focused smokes. |
| `pr_review_key_code_explanation_gap` | A PR review names changed files and symbols, summarizes intent, and lists passing checks, but a reader still cannot reconstruct the critical branch, state transition, side effect, consumer, or failure path from the review. | Exact reviewed head, changed production symbols, surrounding definitions and active call sites, published five-block review, and packet explanation-depth contract. | The review contract asked for mechanism-rich prose but did not make exact-head key-code explanation a required subsection, so agents could satisfy the headings with a polished file inventory. | Require `关键代码讲解` under `具体改动` for code-changing PRs; select 2-5 behavior-bearing symbols, cite exact-head lines, use short excerpts or equivalent pseudocode, and explain inputs/pre-state, branch/invariant, calls/side effects, outputs/consumers, and failure ownership. Keep docs-only reviews on a parallel key-content path and cover both the skill text and CLI packet in the PR-review smoke. |
| `pr_review_proportionality_goal_drift` | A narrow or infrequent failure attracts a large production mechanism; repeated reviews find real implementation gaps, each requested fix grows the PR, and the final review approves because the mechanism is coherent and CI is green even though the maintenance cost no longer fits the original benefit. | Original user-visible problem and recovery cost, first and current exact-head diff shapes, production/state/schema/CLI/caller growth, review findings over time, alternatives and smallest viable fix, and the final proportionality verdict. | Scope-fit proved the code had active callers and code-volume evidence remained descriptive, while verdict policy had no blocking benefit-to-complexity gate. Re-review optimized closure of the latest finding and inherited an approval trajectory instead of resetting from the original problem. | Require typed `change_proportionality` evidence for every code change. Compare verified frequency/severity/blast radius/recovery with production mechanism and long-term maintenance; make `disproportionate` and `not_yet_proven` blocking regardless of correctness or green CI. After material scope growth, reset the whole exact-head review from the original problem and prefer the smallest viable fix, deletion, split, or hold. Cover the projected contract and code-change/docs-only applicability with focused tests. |
| `pr_review_feature_gate_counterfactual_gap` | A review proves the feature-on path and approves an opt-in or default-off change, but users who never enable it still receive new schema requirements, prompt instructions, accepted inputs, persisted projections, scheduling decisions, or effects; scoped sub-agent behavior may also be published under a broader multi-agent protocol name. | Authoritative gate and default, identical disabled/enabled fixtures, pre-change disabled output, every shared changed builder or serializer, emitted schema/prompt/journal/effect differences, public protocol ids, actual actor lifecycle, and granted or excluded authority. | The review inferred whole-change isolation from the absence of one enabled topology or operation list, and treated broad protocol terminology as cosmetic even when it implied registered peers or durable coordination that the implementation did not grant. Feature-on tests then encoded the default-off leakage as expected behavior. | Require typed `default_off_isolation` and `authority_semantics` evidence for every code change, with explicit `not_applicable` only after checking scope. Trace all shared surfaces, run a paired feature-off/feature-on counterfactual, compare the disabled side with the pre-change contract, and block `not_isolated`, `misleading`, or `not_yet_proven`. Prefer established sub-agent/child terminology for ephemeral scoped execution and rename unshipped v0 protocols before compatibility cost accumulates. |
| `slash_command_packet_projection_drift` | A slash command tool is called, but the agent pipes or summarizes its JSON into a narrow view such as `.summary` / `.review_sequence`, then ignores the packet's response contract, templates, evidence commands, or final-answer requirements. | Raw session tool calls, command stdout, slash-command catalog `agent_contract`, full CLI JSON packet, skill fallback instructions, final answer shape. | The agent treated the CLI as a queue/statistics helper instead of the authoritative interaction contract; key machine fields never entered model context. | Make the skill name the exact JSON command that preserves contract/template fields, add machine-readable `required_packet_fields_to_preserve`, forbid summary-only projection for the first pass, and cover the required fields with focused smoke tests. |
| `loop_surface_activation_gap` | A thread reports LoopX setup success because `register-agent` or `quota should-run --agent-id` works, but no Codex App heartbeat automation, Codex CLI `/goal`, or other host loop has been installed for that goal. | `upgrade-plan` / host-loop activation status, `$CODEX_HOME/automations/*/automation.toml`, generated `heartbeat-prompt`, target thread id, recent setup final answer, registry coordination fields. | The control-plane identity layer was treated as the host scheduler layer; setup success criteria stopped at registry/quota instead of proving `host_loop_activation.activated=true` or surfacing a concrete host-tool gate. | Project setup and agent registration must surface `host_loop_activation`; create/update the host loop from generated scoped `heartbeat-prompt`, or report the exact missing host capability. Do not claim "connected" as autonomous until the host loop is active. |
| `codex_app_automation_toml_contract_gap` | A multiline Codex App heartbeat is visible under a permissive line reader, then disappears from upgrade or RRULE resolution after a standards-compliant TOML reader is introduced; prompts containing backslashes, quote delimiters, or escape-like text are common triggers. | Exact writer output, standard-library TOML readback, prompt digest/counts, manifest parse-error projection, and RRULE resolution before and after the reader change. | The writer interpolated arbitrary prompt text into a TOML basic string without serialization, while the old partial reader masked the invalid file and the strict reader's catch-and-skip path erased the difference between absent and malformed automation. | Treat writer and reader as one storage contract: encode every string through a TOML-safe serializer, parse with the standard library at all read sites, fail closed on malformed content, and expose bounded content-free parse diagnostics. Cover backslashes, embedded delimiters, line-ending escapes, exact prompt identity, and capped malformed-file reporting in the real writer-to-reader path. |
| `scheduler_host_update_retry_loop` | The same Codex App heartbeat repeatedly hangs or fails while applying the same recommended RRULE against the same observed host RRULE, including when active-work and monitor-wait targets alternate. | `scheduler_hint.codex_app`, persisted scheduler state, host RRULE observation, recent heartbeat history, host-tool timeout/failure. | Host update failure was treated as turn-local advice or persisted as one scalar, so a cadence phase change overwrote the previous failed pair and the next phase retried it. | Persist a bounded, expiring set of failed target/observed-host pairs with `failure_hint.cli_args`, retain the latest scalar only for compatibility, suppress every retained exact pair without ACK or spend, invalidate old-host entries when the observation changes, and clear the cache after a successful ACK. |
| `scheduler_implicit_host_default` | A generic or CLI caller omits scheduler ownership fields yet receives Codex App RRULE/backoff actions, so tests pass without proving the real host path uses the same contract. | `scheduler_hint.execution_context`, quota CLI argv, heartbeat generator output, host-loop activation packet, scheduler tests. | The shared resolver treated missing context as a legacy Codex App default instead of assigning compatibility at the actual App boundary. | Make missing generic context fail closed; preserve App behavior only through an explicit compact runtime profile emitted by the Codex App generator; pass typed context for other hosts; cover bare failure, profile behavior, generated command, and real CLI integration. |
| `scheduler_followup_runtime_binding_gap` | A Codex App quota decision emits an ACK or failure command, but executing that generated command recomputes a generic fail-closed scheduler packet and rejects the writeback. | `scheduler_hint.codex_app.ack_hint.cli_args`, `failure_hint.cli_args`, the recomputed `before.scheduler_hint.execution_context`, and a real generated-command CLI smoke. | The initial `quota should-run` carried an explicit App runtime profile, but the follow-up command and current-hint handler dropped it during recomputation. | Carry the compact App profile in generated ACK/failure commands, resolve the same typed context in every quota command that recomputes scheduler state, and execute the generated command in a durable CLI smoke before claiming convergence. |
| `scheduler_followup_turn_projection_gap` | A settled receipt-bound heartbeat recommends one host cadence, but its generated ACK/failure command recomputes an unscoped successor or monitor lane with a different reset token; a delayed Todo-less autonomous-replan follow-up may also replay after the Turn has fully settled, and a human gate may push the host past an earlier continuous-monitor deadline. | Originating heartbeat receipt and Turn id, exact ACK/failure argv, binding kind, receipt-bound settlement phase, recomputed selected Todo and scheduler identity, user-channel contract, continuous-monitor cadence/due horizon, and observed host RRULE. | Scheduler follow-ups preserved runtime/registry routing but dropped the originating Turn or treated a settled Todo-less replan replay as fresh scheduler authority, while human-gate notification authority was also treated as cadence authority over tighter monitor deadlines. | Bind the exact `turn_instance_id` into generated ACK/failure commands and rebuild follow-ups through the receipt-bound live-decision path. Once that exact Todo-less replan Turn is durably settled, return a zero-write idempotent follow-up and let a fresh Turn own current cadence reconciliation; preserve ordinary Todo-bound ACK behavior. Cap human-wait cadence with the shared monitor deadline plan while retaining distinct human-gate notification cooldown. Cover exact generated-command success before and after replan settlement, one-spend replay, Todo-bound ACK mutation, ordinary 30-minute human wait, and a three-minute monitor deadline under the gate. |
| `user_gate_host_cadence_spam` | An unresolved user gate is correctly projected, but a failed host cadence update leaves a tight poll and `repeat_until_resolved` sends the same notification every few minutes. | quota interaction contract, scheduler host failure state, desired and observed RRULEs, recent notification cadence. | Gate persistence and per-poll notification delivery were represented by one boolean, so scheduler failure suppression stopped host retries but not user-message retries. | Preserve the open gate and first notice, then derive a notification-only cooldown from the existing failed target/observed-host pair. Quiet short polls outside a host-sized reminder window; reopen on the window, gate identity change, or host RRULE change. |
| `tiny_turn_under_delivery` | Many heartbeats make only one small doc/prose edit despite quota permitting a coherent batch. | recent history durations, active priority stack, user feedback, todo completion rate. | Agent optimized for avoiding mistakes rather than delivering a bounded batch. | Run steering audit, choose a larger verifiable batch, and record why it remains within boundary. Consider cadence/budget policy updates if repeated. |
| `todo_succession_gap` | A non-trivial feature todo is marked done after PR/smoke success, but rollout, product-path audit, docs, telemetry, benchmark proof, or operator-decision follow-up is only mentioned in prose or not recorded at all. | completed todo note/evidence, recent PR/run history, active `Agent Todo`, active `Next Action`, related design docs. | Agent treated an implementation slice as feature completion, or LoopX lacked a reminder to create the successor todo. | Keep the lifecycle simple: do not add many feature states. Complete the old todo and create the next concrete agent/user todo with `--next-agent-todo`/`todo add`, or record a compact no-follow-up rationale. |
| `todo_parent_successor_frontier_gap` | An open parent remains quota-runnable after its work was split into explicit successors, while every unfinished successor is deferred or owned by another peer and the parent has no independent immediate action. | parent status and `successor_todo_ids`, successor ownership/resume conditions, `todo update` response `parent_successor_advisory`, current quota candidate. | Successor links were mistaken for suspension or aggregate lifecycle state even though they intentionally encode lineage only. | Do not infer aggregation from successor links. At the authoring boundary, require the agent to decide whether the parent retains an independent immediate action; if not, explicitly defer it with a supported `resume_when` or complete it. Keep the reminder advisory and machine-readable rather than adding persisted aggregate state. |
| `integration_remote_head_observation_gap` | `integration-branch status/sync --refresh-remotes` reports `in_sync`, but a normal push is rejected because another agent advanced the published integration branch. | integration plan, configured branch upstream, local and remote integration SHAs, merge-base relation, normal push result. | Remote refresh observed only base/source inputs, while tree-equivalent local candidates were treated as sufficient even when they omitted the published integration ancestry. | Narrow-fetch the configured integration upstream with base/source refs, surface remote-ahead or diverged ancestry as typed drift, and require automatic or supplied candidates to contain that exact remote head before recording an in-sync receipt. Never force-push or prune unrelated refs. |
| `plan_todo_writeback_gap` | The agent explains a route change, top-20 todo list, P0/P1 plan, or deprecation policy to the user, but the active LoopX todos / Next Action / refresh-state payload do not contain the same commitments. | latest user-facing plan, quota `todo_write_hint`, active `Agent Todo`, `User Todo`, `Next Action`, recent refresh-state runs. | Agent used chat as memory after understanding the plan; LoopX prompt/skill did not make plan publication a writeback trigger. | Treat user-facing LoopX plans as writeback triggers: add/update concrete todos or refresh state before final, or explicitly mark the plan speculative/no-writeback with rationale. Add skill/prompt wording when this recurs. |
| `gated_unmerged_change_continuity_gap` | Work is prepared, staged, or committed locally while a repository change window blocks commit or push, but the durable state records only a future reminder; work that never attempts a Git operation is especially easy to miss. After restart or handoff, no global surface can identify the producing repository, checkout/head, worktree, changed-path inventory, validation, or terminal outcome. | change-window decision, pending-change global projection, private locator health and path count, repository checkout/head and worktree fingerprint, linked Todo, validation and PR refs, plus an explicit linked-worktree reconciliation preview when broad recovery is requested. | Schedule enforcement and work continuity were modeled as separate prose conventions, and hook denial was the only enrollment seam, so correctly paused work could silently orphan the code needed after the gate reopened. | On a denied repository hook, append or idempotently refresh a public-safe pending-change event. At bounded writeback or handoff during a blocked window, reconcile the current dirty checkout even when no Git operation was attempted; keep a broad linked-worktree sweep explicit and treat unattributed historical dirt as candidates rather than proof of current-gate causality. Store repository identity, branch or detached checkout/head, content-free fingerprint, next eligible time, and counts in the shared projection; retain the worktree and repository-relative changed paths only in a mode-0600 private locator. Allow explicit goal/Todo/write-scope/validation enrichment, expose cross-project list/verify, and require typed merged/superseded/abandoned resolution. Never copy code, diff bodies, credentials, file names, or personal paths into the shared projection. |
| `capability_route_hidden_in_prompt` | A caller explicitly selects a registered capability, but `start-goal` exposes the route only in planning prose; or the first turn qualifies correctly while a later heartbeat loses the route and writes a plausible generic implementation Todo. | exact start-goal route switch, full and compact packets, guided `ordered_steps`, capability admission state, projected successor Todo, later quota `selected_todo`. | Capability selection remained optional prose, or a bootstrap-only route was mistaken for durable later-turn authority. | Project the explicit switch as a typed bootstrap route and pre-implementation guard, persist admission in capability-owned state, then write its exact generic successor with `action_kind` plus a stable public `target_key`. Preserve those scheduling fields in later quota selection; do not copy domain facts into Todo or infer admission from stale prompt context. Cover switch-off/on, compact/full parity, exact successor validation, and later-turn quota re-entry. |
| `capability_owned_transition_bypass` | A selected capability implements a typed external transition, but the agent calls the provider CLI directly and only later tries to retrofit capability state; for issue-fix this can appear as direct issue creation for a reproduced `discovered-*` defect. | selected capability route, `capability show` command list, implemented protocol list, generated skill, and a content-free external-write trace. | The transition existed in code/docs but was absent from the model-facing capability catalog, while the generic skill did not make the capability command surface authoritative for later transitions. | List every capability-owned external transition in `capability show` and make the generic skill use capability commands instead of substitute provider CLIs. Keep domain facts out of generic Todos; cover the catalog and generated skill without growing hot-path packets. |
| `capability_route_false_positive` | A broad engineering Goal mentions fixes and a PR lifecycle, but `start-goal` selects `issue-fix` and the host then rejects activation because no issue admission binding exists. | exact start-goal argv, `selected_capability_route`, host activation error, and whether an explicit product-route switch was present. | Product capability selection was inferred from natural-language Goal text, so harmless wording became control-plane authority. | Make product-route selection an explicit start-goal switch; without it, no wording, URL, or semantic match may select the capability. Keep capability-owned admission as the separate execution authority. Cover switch-off text contrasts, switch-on route projection, rerun-command preservation, and compact/full parity. |
| `connector_runtime_boundary_gap` | A real browser/API connector run loads source bodies, message lists, derived reports, media, or engagement data before the owner gate or metadata-only policy is enforced. | connector packet, runtime/network trace summaries, source status, owner gate todo, runtime policy fields, protocol docs. | Connector safety lived in prose or posthoc packet fields, but the runtime route itself could autoload raw material before the agent intervened. | Add a machine-readable connector runtime policy with safe default, browser-open rule, allowed probes, and forbidden URL/path prefixes; smoke that private connectors remain gate-only and public metadata connectors stay bounded. |
| `openviking_peer_actor_scope_gap` | An OpenViking provider doctor succeeds, but a real scoped recall fails with `PERMISSION_DENIED` while the equivalent explicit peer-scoped `ov find` succeeds. | provider binding, target `viking://.../peers/<peer>/...` scope, emitted OpenViking CLI argv, doctor result, scoped recall result. | The generic provider bound the target URI but omitted the verified actor peer identity required by OpenViking's peer isolation boundary. | Carry an explicit compact actor peer id through the provider binding and prefix only scoped provider operations with `--actor-peer-id`; never infer or impersonate an actor from an arbitrary target URI. Cover unscoped compatibility plus peer-scoped search/read argv. |
| `benchmark_product_path_mismatch` | Treatment uses a surrogate state file, partial lifecycle prompt, or route that differs from the real Codex App + LoopX path, making uplift attribution unstable. A common subcase is "lifecycle touched" evidence without a complete case closeout/spend event. | benchmark runner inputs, compact CLI subcommand counters, state file paths, installed skill/prompt snapshot, public lifecycle trace counts. | Benchmark route drifted from product route, or the treatment prompt described closeout as optional while the reducer required a full LoopX turn. | Add posthoc parity checks for state path, CLI interaction trace, heartbeat/state/skill inputs, protocol version, and closeout counters before claiming result. Make the prompt and reducer agree on the canonical closeout sequence: `todo complete`, `refresh-state`, then `quota spend-slot --source adapter --execute`. |
| `benchmark_source_revision_admission_gap` | A long-running campaign pins a clean source checkout at startup, then keeps admitting new runs after the tracked reference advances; operators must remember to rebuild or reinstall manually. | Exact local source revision and cleanliness, intended pin, a freshly observed authoritative reference revision immediately before admission, and the immutable revision already bound to each in-flight run. | Startup pinning proved reproducibility for the first admission but was reused as freshness evidence for later admissions. | Before every new run admission, freshly observe the authoritative reference through the runner/provider boundary and require the built-in benchmark source-revision fence to prove clean local revision = intended pin = observed reference. Fail closed and refresh the runtime on mismatch; never rewrite already admitted runs or grant LoopX core fetch, install, or launch authority. |
| `benchmark_adaptive_concurrency_benign_churn_stall` | A saturated campaign stays healthy, but ordinary qualified completions and successful refills continually change the envelope revision, so the tuner never accumulates enough healthy windows to probe the next target. | Exact terminal qualification, release and refill receipts, pre/post envelope revisions, fresh feedback streak, and launch/provider/runner/resource pressure counters for the whole window. | Receipt revision fencing correctly blocked replay, but the runner also treated every revision change as a health-streak reset, conflating ordinary lifecycle churn with pressure. | Keep old receipts invalid after every mutation. Preserve only the runner-owned healthy streak across a qualified terminal plus successful refill with no pressure signal, then issue fresh feedback bound to the post-refill revision. Reset on failed refill, unresolved terminal state, launch/provider/runner failure, or resource pressure; cover both stale-receipt rejection and fresh-feedback increase after benign churn. |
| `benchmark_countable_success_uncommitted_turn` | An official benchmark result is countable and passes, but the treatment reports zero committed/proven Turns; public receipts show a progressful host idle failure followed by repeated pre-host planning failures. | official score/countability, compact Turn executions and readiness blockers, public bridge progress counts, continuation/handoff scheduling flags, typed repair counters, and public-safe command failure categories. | Progress validation handoff required an exhausted resumable session even when no resume session existed; generic host failures omitted their typed result kind, and receipt-only repair failures were invisible to the bounded repair controller. | When safe task-facing progress exists and no same-session continuation is scheduled, hand it to the independent scored-workspace validator. Persist the generic `host_failure` result kind, recognize receipt-level repair/replan outcomes, and route one bounded typed repair instead of polling the same failed frontier. Keep official countability distinct from Turn readiness and require a committed, independently validated Turn before claiming treatment qualification. |
| `benchmark_integrity_suspicion_verdict_collapse` | A trajectory scanner flags a restricted-source request or host-boundary escape marker, and the run is immediately labeled cheating or removed from score claims even though isolation blocked the request, the marker only observed the current namespace, no restricted content was returned, or no causal benefit is established. | Scanner categories, real solver trajectory, tool results, final workspace, runtime isolation attestation, and post-run analyst decision; keep raw evidence private. | Automated detection and causal adjudication were collapsed into one fail-closed verdict, so a weak suspicion signal silently overrode the actual information flow. | Keep structural audit failures, missing runtime attestations, and observed credential exposure fail-closed, but represent restricted-resource access and host-escape matches as countable `suspected` state. After solver and score terminal, require an agent to review the real trajectory, tool results, and final workspace. Confirm cheating only when restricted material was actually disclosed and causally entered solving or validation; otherwise retain the score with an audit warning. Emit only compact reason codes and a public-safe evidence pointer. |
| `model_qualification_without_provider` | An adapter unit test or fixture-actor portfolio passes and is reported as proof that Doubao follows a changed behavior contract, even though no Ark request occurred for the candidate commit. | actor transport type, provider call count, runtime credential preflight, bounded live receipt source identity, PR validation summary. | Adapter/harness correctness and model-behavior qualification were collapsed into one test label; the live route was ad hoc or allowed missing credentials to look like a skip. | Keep doubles only for adapter and oracle regression coverage. Provide a fail-closed live command that uses shipped packet builders, requires runtime-injected credentials, records real provider call counts in a bounded receipt, and forbid any Doubao-pass claim without that receipt for the candidate commit. |
| `action_signature_coverage_drift` | A new model-facing action field is included in the canonical action signature, but the signature still advertises the previous coverage version and base/head qualification passes. | signed action document, emitted `action_signature.coverage`, semantic digest receipt, base/head differential, focused blocking-state fixture. | The signed document and its coverage declaration evolved in separate layers, while the differential retained only a digest and could not recognize declared migrations. | Version coverage when a signed semantic dimension is added, preserve the old version for unchanged envelopes, carry coverage in differential receipts, allow only reviewed version transitions, and keep unversioned digest changes fail-closed. |
| `benchmark_prompt_path_override` | An agent reports writing a requested relative output, but the verifier cannot find it at the task-declared location; the bridge trace shows the file was written under a fixed root instead. | task path instructions or visible metadata, treatment prompt, compact bridge operation summary, structured verifier failure category. | A benchmark harness prompt overrode task-relative path semantics with a hard-coded sandbox root. | Centralize one output-path contract that preserves explicit absolute paths and resolves relative paths from their declaring metadata or task working directory; forbid fixed-root coercion and cover both treatment prompt surfaces with focused smokes. |
| `benchmark_aggregate_ledger_split_brain` | A successful benchmark closeout makes canonical aggregate coverage or countable totals drop sharply even though the canonical ledger is unchanged. | launcher dry-run, resolved ledger and aggregate paths, closeout sync summary, canonical ledger run/case counts, aggregate dry-run from the canonical ledger. | An explicit aggregate path was combined with an implicit worktree-local ledger path, so a partial catch-up ledger overwrote the canonical aggregate. | Require an explicit ledger whenever an aggregate path is explicit, print both resolved paths in dry-run output, and rebuild the aggregate from the intact canonical ledger before continuing launches. |
| `benchmark_supervisor_terminal_closeout_gap` | Public supervisor liveness remains nonterminal after the detached supervisor process disappears, and no compact terminal result can arrive. | Public liveness timestamp and state, content-free process presence, and compact public artifacts only; do not read raw task text, logs, trajectories, verifier output, credentials, or private runner values. | An unhandled supervisor exception or normal termination signal bypassed the ordinary `finish()` path, so the process exited without replacing its last running checkpoint. | Catch unexpected failures and normal termination signals at the supervisor entry boundary, emit a terminal public-safe fallback with only an allowlisted error or signal type and compact previous-liveness fields, attempt bounded remote cleanup, and cover both paths with focused regressions. The monitor must record the blocker and a repair successor before any rerun. |
| `benchmark_exec_capture_rebind_drift` | A setup-ready host-local or split-control benchmark fails at `docker_exec_capture_preflight`; public receipts show a configured sandbox bridge but no agent operation trace, verifier attempt, or official score. | Public compact prerequisites for capture status, lifecycle guard, rebind, bridge configuration, failed stage, and attempt accounting; do not read raw command output. | Output capture was installed while creating the environment, then the benchmark runtime rebound `env.exec`; a stale installation marker prevented the wrapper from being restored at the agent boundary. | Track the installed wrapper identity, reinstall when `env.exec` changes, ensure capture again at the install-agent boundary, project the rebind decision publicly, and cover late rebinding with a focused synthetic regression before rerunning a case. |
| `benchmark_venv_pip_entrypoint_loss` | A Docker task creates and activates a venv, upgrades pip successfully, then a later bare `pip install` falls back to the system interpreter and fails under PEP 668 before the agent starts. | staged Dockerfile, failing build step, prior successful layer, venv module/console-script presence, public task-staging metadata. | Upgrading pip removed or failed to recreate the venv console script; PATH then resolved the bare command to system pip even though `python -m pip` in the venv remained usable. Incidental task-mount metadata may also shadow the specific pip failure in attribution. | In staged Dockerfiles that explicitly activate a venv, normalize bare pip install commands to `python3 -m pip install`; preserve correct module invocations, uv pip, absolute paths, heredocs, and original task sources. Require concrete mount-error phrases before assigning a mount failure so specific compose/pip evidence wins. |
| `benchmark_launcher_sandbox_mode_drift` | A host-Codex benchmark exits before case materialization with a sandbox-probe failure even though the same runner and credential worked in a prior batch. | launcher dry-run, requested/effective host Codex sandbox mode, preflight category, runner source fingerprint. | The public launcher did not forward the sandbox mode used by the validated runner profile, so the runner fell back to an unsupported host sandbox probe. | Expose and forward the host Codex sandbox setting without weakening its default; make dedicated runners opt into any broader mode explicitly and record only the mode label in public prerequisites. |
| `user_reward_lesson_projection_gap` | The user explicitly corrects a high-value product route, priority, or operating rule, but later turns follow an older todo/recommended_action as if the correction never happened. A recurring form is an owner granting a broad operating permission, then a later agent asks for the same approval because the earlier decision was stored as chat prose or a completed `user_action`. | recent user correction, completed user decisions, latest quota/status, active-state `Next Action`, open todos, recent run history, interaction pattern catalog, and projected standing decision authority. | The correction stayed in chat/model belief instead of becoming a durable human-reward/operating-lesson constraint, successor todo, or state projection; or a standing permission was encoded in a non-authoritative reminder lane that quota intentionally ignores. | Promote route and priority corrections into active goal state and runnable todos. When the correction is a reusable product policy, encode it in the capability-owned typed contract and keep Todo or monitor prose from becoming a competing authority. For recurring authority, author a completed broad `user_gate` with explicit approve/reject/cancel, exact decision scope, and agent/global ownership; make privileged agent todos declare the matching required scope; project the latest typed receipt before quota consistency; retain it through archive compaction. Never infer authority from chat or `user_action` prose. Then refresh state so `quota should-run` selects the corrected rule, and validate the hot path before continuing. |
| `reward_feedback_leak` | Later benchmark rounds receive verifier tail, pass/fail, or reward details that one arm should not see. | benchmark prompt/continuation logs, compact ledger, verifier wrapper. | Evaluation loop leaked reward signal. | Record per-round reward privately for metrics, but do not feed it to agents unless the experiment explicitly studies feedback. |
| `candidate_preflight_negative_evidence_gap` | Issue-fix candidate screening accepts empty PR evidence and starts implementation even though the caller used a capped aggregate index or did not prove a direct all-state search. | Candidate preflight input, issue-specific numeric and semantic query receipts, truncation/completeness metadata, current issue body and comments. | Capability admission treated key presence or a naked empty list as proof that prior work was absent. | Keep provider queries outside the LoopX core, but require issue-specific complete, non-truncated receipts before a negative result can yield `proceed`; aggregate indexes remain candidate generators only. Do not add capability fields to generic Todos. |
| `commit_hygiene_drift` | Broad commit includes temporary smokes, raw logs, local state, or unrelated docs. | `git status`, `git diff --stat`, `git ls-files --others --exclude-standard`, AGENTS.md. | Worktree was staged by chronology rather than reviewer logic. | Use explicit pathspecs, split commits, keep only durable smokes, and update AGENTS/skill if the failure mode recurs. |
| `migration_terminal_receipt_replay_gap` | A migrated transaction succeeds once, then an idempotent legacy retry is rejected as receipt corruption or times out on a lock that the first response lost. | Versioned operation receipt, persisted state, lock owner/token, exact caller retry identity, pre-migration replay behavior, and focused native plus compatibility tests. | The new owner modeled only held/committed receipts and treated a closed no-op receipt as incomplete, or generated retry identity inside the callee after the caller's retry boundary. | Model terminal no-op receipts separately from held authority proofs, replay them without retired private tokens, and generate one stable operation id outside any transport retry while minting a new id for independent calls. Cover direct native replay and the compatibility adapter's response-loss boundary. |
| `typed_kernel_import_surface_drift` | Focused tests and premerge canaries pass, but CI mypy starts reporting unrelated errors in a previously unchecked module after a small typed-kernel change. | Exact mypy entry files, the new import edge, base/head module diffs, and whether the imported module owns the new behavior or is only a type source. | A directly checked kernel module imported a broad runtime owner for one enum or provider-selection helper, silently expanding strict mypy traversal and placing single-caller policy in the wrong bounded context. | Keep provider selection in its nearest caller, leave provider-neutral builders in the typed kernel, and avoid importing a broad runtime owner only to expose one caller-local decision. Run the exact CI mypy command after changing imports in configured kernel files. |
| `packaged_surface_completion_illusion` | A development route or API-mocked browser smoke passes while the packaged product still serves an older UI, loses local history after refresh, hides every healthy Agent, or records important contradictions only as non-failing observations. | packaged entry title and first viewport, emitted bundle identity, real loopback capabilities, service-restart history, Last-Event-ID replay, mobile navigation close behavior, and the acceptance report's untested/observation fields. | UI completion was inferred from component tests or a Vite route; the release asset, launcher environment, durable local session path, and hard browser assertions evolved separately. | Make packaged-route parity a blocking acceptance contract. Build and launch the real bundle with a controlled executable path, convert semantic observations into assertions, persist status-only and Agent-backed messages independently, test refresh plus restart recovery, and require every design criterion to be PASS or explicitly gated before claiming the surface complete. |
| `dashboard_status_source_generation_gap` | Switching between local and SSH sources leaves the old Goal list visible, or the selector and route oscillate between two sources after a slow tunnel ensure or status response. | User selection order, tunnel-ensure start/finish order, status fetch start/finish order, requested and loaded URLs, projection revision, route commits, and the rendered Goal identity. | Request freshness starts only after SSH preparation, a foreground request captures its revision before advancing it, or an older route commit clears a newer pending selection. | Fence the complete selection transaction—from user intent through tunnel ensure, fetch, payload commit, and route commit—with one monotonic selection generation plus a projection revision. Let only the current generation clear pending state; reject background responses whose target is no longer the committed source. Cover delayed A/B fetches and delayed SSH-then-local selection with a focused browser smoke that asserts payload, selector, and route remain aligned. |
| `docs_surface_sprawl` | Root docs become hard for contributors to navigate; research/drafts/history compete with stable contracts. | `docs/README.md`, root docs count, docs governance smoke. | Documentation lacks audience and lifecycle ownership. | Move archive/outreach/research/reference material into indexed subdirs and keep new docs linked from an index. |
| `external_wait_typed_repeat_obligation_gap` | An agent waits on an external gate (for example a PR review) and records the same typed `blocked` observation twice; `quota should-run` then stays `autonomous_replan_required` with a `typed_progress_repeat` trigger even after the blocker is reported, and the pi/generic_cli host loop thrashes on `repair_interaction_contract_projection` because the interaction contract becomes `delivery_allowed=true, must_attempt=false`. | quota `autonomous_replan_decision` + `autonomous_replan_obligation` (trigger kind, progress_baseline fingerprint), `interaction_contract.agent_channel`, `scheduler_hint.consistency_error`, run-history `progress_observation` fingerprints, heartbeat `recommended_mode`. | Repeated identical typed observations on an external wait are a stall signal that creates the replan obligation; `enforce_open_replan_writeback` only accepts a new typed semantic delta (or fresh evidence-linked vision path) in the same writeback, so a repair-delta/blocker-todo claim alone, or a repeated baseline observation, is rejected and cannot clear the obligation. | For external waits use `quota monitor-poll` (no-spend quiet evidence) or record a typed observation at most once per distinct state; never repeat the same fingerprint. When a repeat obligation is already open, close it in ONE atomic refresh: `loopx refresh-state --autonomous-replan-recorded --repair-delta-kind <accountable kind> --progress-result-class blocked --progress-blocker-id <new id, different from the obligation baseline> --progress-evidence-id <new evidence>` (keeping the matching scoped blocker todo open), so the same writeback carries both the accepted typed delta and the repair evidence; do not split the delta and a later fingerprint change across refreshes, because the later refresh still sees the open obligation and is rejected. |
| `long_todo_chain_level_trigger_gap` | A lane remains at or above the long-chain threshold, an accepted replan writeback is durable, yet every heartbeat recreates the same replan obligation until Todos are manually pruned below the threshold. | Agent-scoped source Todo ids/status/owners/update revisions, derived trigger checkpoint, obligation id, accepted semantic ACK, and the next quota decision. | Threshold state was evaluated as a level on every read. The obligation identity omitted the Todo frontier revision, and the ACK did not retain an exact trigger checkpoint, so closed and materially changed frontiers were indistinguishable. | Derive a typed agent-scoped Todo-frontier revision, include it in obligation identity, and copy the exact trigger checkpoint into accepted ACKs and durable compaction. Suppress the long-chain rule while that checkpoint matches; rotate and re-arm only after a material Todo revision. Fail closed when source revisions are incomplete, and fence transition-derived successor ACKs against the current frontier update time. |
| `lark_configured_chat_topic_filter_gap` | A `configured_chat_all` connection can read a message through provider history, but a real-time message from a new topic or topic reply in the same configured chat is absent from the Goal inbox and diagnostics report `topic_mismatch`. | Consuming bot target, configured chat id, incoming chat/root ids, capture scope, route decision reason, and persisted inbox message ids. | The router applied one presentation topic root as an ingress filter before evaluating chat-wide capture scope; profile polling could also route against bindings owned by another bot target in the same chat. | Scope polling to the consuming target first. Prefer an exact topic match; otherwise allow one unambiguous chat-wide route for any root in the configured chat, and fail closed when multiple chat-wide routes remain. Treat the topic root as reply/presentation context rather than an ingress boundary under chat-wide capture, and cover new topics plus multiple bots with focused tests. |
| `lark_inbox_reply_placement_gap` | A top-level chat request is answered with an unnecessary new topic, or structured reply text loses line breaks. | Source `parent_id`/`root_id`, configured placement policy, provider send command, and readback text. | The reply path always forced thread mode and flattened all whitespace before sending. | Resolve placement from the captured source context, retain a compatibility policy for existing configs, preserve line breaks for structured replies, and verify both top-level and in-topic sends with provider readback. |
| `lark_configured_chat_addressing_projection_gap` | `configured_chat_all` reports `reply_due` for a message addressed to another member, Bot-authored prose, or a historical invitation merely because the body also contains the product name; genuine structured Bot mentions may meanwhile lose their addressing evidence after persistence. | Provider-native `mentions`/`mentioned` fields, sender/reply verification summary, canonical event `addressed_to_bot`, content-free urgency counts, and source message settlement. | Provider mention evidence was discarded at the compact inbox boundary, then urgency reconstructed intent from a broad text heuristic such as any `@` plus a product-name token. | Normalize provider-native addressing once into a compact typed flag, persist that flag without raw identities, and make configured-chat urgency accept only the flag or a verified Bot reply. Preserve structured negative mention evidence during normalization, require message-context lookup when the event stream lacks it, and make legacy events without the flag fail closed to material review. Cover another-member mentions, Bot-name prose/self messages, verified direct mentions, and legacy events with public-safe fixtures. |

| `repository_delivery_gate_projection_gap` | Git rejects commit or push through an effective global or repository-local guard while quota still exposes only broad `delivery_allowed=true`, so preparation eligibility is mistaken for repository-delivery admission. | Path-free `change-window status` diagnostic, provider verification checks, typed policy decision, interaction-contract repository delivery gate, typed hook registration/result, linked-worktree and separate-clone readback. | Provider discovery exposed only repository-local install state, while Kernel interaction state had no trusted, provider-neutral seam for a capability-owned commit/push decision. | Detect configured external guard surfaces without returning paths or inferring policy; recognize only a bounded legacy signature for preview-first layering. Register a bounded read-only interaction projection hook at the composition root; keep typed validation, slot conflicts, and failure isolation core-owned. Project commit/push admission only from a fully verified typed repository provider, keep preparation/validation distinct, carry `next_eligible_at`, and never grant remote-write authority. |
| `dashboard_verified_mutation_projection_gap` | A preview-locked dashboard mutation reports a successful shared-state readback, but the initiating control still shows its old value; a second click then says the requested setting already exists. | Exact apply receipt, no-change canonical preview, shared-state readback verification, status projection generation/revision, rendered control state, and refresh outcome. | The data adapter verified the canonical write or no-change state, then the UI discarded that receipt and rebound immediately to a separate stale status projection. | Return the verified configuration through apply and no-change preview callbacks and use it as a drawer-scoped read model for the same Goal; refresh the normal projection independently, surface refresh failure without undoing the verified result, and clear the override when the drawer selection changes. Keep preview state visibly pending rather than presenting it as applied. Cover a deliberately stale status response in the browser smoke. |
| `dashboard_open_token_picker_gap` | A bounded dashboard setting asks users to type protocol tokens such as `task_domain`; the placeholder looks like a current value, users cannot discover legal choices, or Goals without tagged work cannot enable a capability whose runtime treats the token filter as optional. | Current Goal Todo index, configured token allowlist, option-to-Todo match counts, canonical empty-filter semantics, preview payload, empty state, and packaged browser behavior. | An intentionally open optional backend vocabulary was exposed as a required product authority boundary; reading only a compact Goal-card Todo slice can also hide valid choices. | Keep the open typed token contract in the owning control-plane boundary, but present it as an optional per-Goal multi-select derived from the authoritative Todo index plus already configured values, using compact Goal Todo rows only as a compatibility fallback. Empty means no token filter while every independent admission boundary remains enforced; a non-empty selection remains a strict allowlist. Show match counts, preserve configured zero-match values, and cover unrestricted, restricted, invalid-token, preview, readback, empty-state, and packaged parity behavior. |

## Minimal Evidence Packet

Desktop runtime preparation failures must remain inside the live startup
supervisor. Compare the App's bundled revision, selected CLI revision and
listener identities independently; a working terminal CLI is not proof of a
working Finder launch. Reproduce installer validation with Finder's minimal
PATH and isolated install/registry/runtime directories. Use a shared bounded
tool search for installer and child services, retain exact revision checks,
bound automatic installation retries, and prove failed preparation followed by
repair reaches service startup without recreating the App process. Only an App
binary replacement requires restart; runtime repair resumes the same window.
Installation readiness must not traverse registered Goal workspaces or run
host/provider integration checks. Use `doctor --deep --installation-only` to
retain package/toolchain validation without waiting on user-folder consent;
full operator diagnostics remain available through ordinary `doctor`.
Prove HTTP readiness independently of optional integration discovery: a bound
port may still have no serving loop. Block the snapshot reader in a real-server
test, verify workspace/capability responses and shutdown, and fence late
results from starting consumers or resuming queues after close. Distinguish
silent sockets from foreign HTTP services; recovery must retain process and
port ownership verification before restarting an unresponsive listener.

For most repairs, capture:

```text
goal_id:
observed_surprise:
quota_state:
interaction_contract:
user_todo_open_count:
agent_todo_open_count:
recommended_action:
goal_boundary_write_scope:
active_state_next_action:
recent_history_summary:
responsible_layer:
repair:
validation:
```

Keep this packet compact and public-safe. Store only summaries in public docs;
raw logs and private traces stay in ignored local paths.

| A prompt migration reports success but the App restores the old body | A running host caches automation rows; disk CAS cannot invalidate scheduler memory. Use native automation_update and read back through that host, or close the host for offline migration and verify after restart. | Keep update-time reconciliation fail-closed while the App runs, and cover unchanged SQLite/TOML plus a complete native update request. |
| A newly started heartbeat persists execution rules despite a successful runtime update | Startup and migration used different renderers, and installed skill guidance still told the agent to save raw thin output. Generate the v2 loader from the activation command and replace stale lifecycle instructions. | Exercise onboarding to bootstrap to one-level fresh CLI load, authentic legacy migration, and absent/duplicate-command rejection. |
